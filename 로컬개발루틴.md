http://213.181.122.2:47556
qwen3-coder-64k

엔드포인트 URL: https://heat-independence-starts-enclosure.trycloudflare.com
모델명: qwen3-coder-64k
API 키: sk-redsky-1234

# 로컬 개발 루틴 (Vultr DB 연결)

## 시스템 구성

```
브라우저
  └─ http://localhost:5173 (Vite 개발 서버)
       └─ /api/* 프록시
            └─ Node 서버 http://localhost:4000
                 └─ PostgreSQL localhost:5432 (SSH 터널)
                      └─ Vultr 서버 141.164.35.124:5432 (실제 DB)
```

---

## 출근 루틴 — 터미널 3개 순서대로 실행

### 터미널 1 — SSH 터널 (Vultr DB 연결)

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify
.\ssh-tunnel-vultr.ps1
```

> "Vultr DB SSH 터널 시작..." 메시지가 뜨면 정상.
> 이 터미널은 개발 중 계속 열어두어야 합니다. Ctrl+C 하면 DB 연결이 끊깁니다.

---

### 터미널 2 — Node API 서버 실행

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify\server
npm run dev:vultr
```

> `[nodemon] starting 'node src/index.js'` 메시지와 함께 포트 4000에서 대기.
> `.env.vultr` 설정을 읽어 Vultr DB(localhost:5432 터널 경유)에 접속.

---

### 터미널 3 — 프론트엔드 실행

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify
npm run dev
```

> `Local: http://localhost:5173/` 확인 후 브라우저에서 접속.

---

## 정상 동작 확인

| 확인 항목 | 주소 / 방법 |
|-----------|-------------|
| 프론트엔드 | http://localhost:5173 |
| API 서버 헬스체크 | http://localhost:4000/api/health (또는 /api 엔드포인트) |
| DB 연결 확인 | DBeaver로 localhost:5432 접속 (터널 켜져 있을 때만 가능) |

---

## 퇴근 루틴 — 종료 순서

1. 터미널 3 (프론트) → `Ctrl+C`
2. 터미널 2 (서버) → `Ctrl+C`
3. 터미널 1 (SSH 터널) → `Ctrl+C`

> SSH 터널을 마지막에 닫아야 서버 종료 시 DB 연결 오류 로그가 남지 않습니다.

---

## DBeaver로 Vultr DB 직접 접속할 때

SSH 터널(터미널 1)이 켜져 있는 상태에서만 접속 가능.

| 항목 | 값 |
|------|----|
| Host | localhost |
| Port | 5432 |
| Database | peoplify |
| User | postgres |
| Password | peoplify |

자세한 설정 방법 → [DBeaver사용법.md](DBeaver사용법.md)

---

## 자주 발생하는 문제

### SSH 터널 연결 실패

```
ssh: connect to host 141.164.35.124 port 22: Connection refused
```

→ Vultr 서버가 꺼져 있거나 SSH 포트가 막힌 경우. Vultr 콘솔에서 서버 상태 확인.

---

### 서버 실행 시 DB 연결 오류

```
error: connect ECONNREFUSED 127.0.0.1:5432
```

→ SSH 터널(터미널 1)이 실행되지 않은 것. 터미널 1을 먼저 실행한 후 서버를 재시작.

---

### 프론트에서 API 호출 시 Network Error

→ Node 서버(터미널 2)가 실행 중인지 확인. `http://localhost:4000` 직접 접속해서 응답 확인.

---

## 참고 파일

- [로컬에서서버실행방법.md](로컬에서서버실행방법.md) — Docker 기반 실행 방법 (Vultr 대신 로컬 Docker DB 사용 시)
- [vultr서버관련.md](vultr서버관련.md) — Vultr 서버 설정 및 관리
- [DBeaver사용법.md](DBeaver사용법.md) — DB 직접 접속 방법




# 내집 nas db에 연결 개발 루틴-----------------------------------------


## 출근 루틴 — 터미널 3개 순서대로 실행

### 터미널 1 — SSH 터널 (nas DB 연결)

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify
.\ssh-tunnel-nas.ps1
```

---

### 터미널 2 — Node API 서버 실행

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify\server
npm run dev:remote
```

> `[nodemon] starting 'node src/index.js'` 메시지와 함께 포트 4000에서 대기.
> `.env.remote` 설정을 읽어 Nas DB(localhost:5433 터널 경유)에 접속.

---

### 터미널 3 — 프론트엔드 실행

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify
npm run dev
```

> `Local: http://localhost:5173/` 확인 후 브라우저에서 접속.

---
