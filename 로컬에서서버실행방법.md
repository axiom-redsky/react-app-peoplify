# Peoplify 로컬 개발 환경 실행 가이드

## 📐 시스템 구성

```
브라우저
  └─ http://localhost (포트 80)
       └─ nginx (Docker)
            ├─ /          → React 정적 파일 (빌드 결과물)
            └─ /api/*     → server:4000 (프록시)
                               └─ PostgreSQL DB (포트 5432)
```

---

## 🛠️ 사전 준비

### 1. Docker Desktop 설치

> 이미 설치된 경우 건너뜁니다.

1. https://www.docker.com/products/docker-desktop 접속
2. **Windows** 버튼 클릭 후 설치 파일 다운로드
3. 설치 후 **PC 재시작**
4. Docker Desktop 실행 — 트레이 아이콘이 초록색이면 정상

설치 확인:

```powershell
docker --version
docker-compose --version
```

### 2. Node.js 설치 (방법 B 사용 시 필요)

> Docker 방식(방법 A)만 사용할 경우 불필요

- https://nodejs.org 에서 **LTS 버전** 설치 (v20 이상)

설치 확인:

```powershell
node --version
npm --version
```

---

## 🚀 실행 방법 A — 전체 Docker (가장 간단)

프론트엔드, 서버, DB 모두 Docker로 실행합니다.

### 접속 주소

| 서비스 | 주소 |
|--------|------|
| 앱 (프론트) | http://localhost |
| API 서버 | http://localhost:4000 |
| PostgreSQL DB | localhost:5432 |

### 실행 순서

**1단계 — 프로젝트 루트 폴더로 이동**

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify
```

**2단계 — 전체 빌드 및 실행**

```powershell
docker-compose -f docker-compose.local.yml up --build
```

> - 최초 실행 시 이미지 빌드로 5~10분 소요
> - DB 마이그레이션, 시드 데이터 삽입이 자동으로 실행됨
> - 이후 실행 시 `--build` 생략 가능 (코드 변경 시에는 필요)

**3단계 — 브라우저 접속**

```
http://localhost
```

**로그인 계정**

| 이메일 | 비밀번호 | 권한 |
|--------|---------|------|
| admin@peoplify.com | password | 관리자 |
| user@peoplify.com | password | 일반사용자 |

### 종료

```powershell
# 컨테이너만 종료 (DB 데이터 유지)
docker-compose -f docker-compose.local.yml down

# DB 데이터까지 완전 삭제
docker-compose -f docker-compose.local.yml down -v
```

---

## 🚀 실행 방법 B — 하이브리드 (DB만 Docker, 나머지 로컬)

코드를 수정하며 개발할 때 권장하는 방식입니다.
프론트, 서버는 핫리로드가 적용되고 DB는 Docker로 안정적으로 유지됩니다.

### 접속 주소

| 서비스 | 주소 |
|--------|------|
| 앱 (프론트) | http://localhost:5173 |
| API 서버 | http://localhost:4000 |
| PostgreSQL DB | localhost:5432 (Docker) |

### 실행 순서

터미널 3개를 준비합니다.

---

**[터미널 1] DB만 Docker로 실행**

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify
docker-compose -f docker-compose.local.yml up db -d
```

---

**[터미널 2] 서버 실행**

최초 1회만 — 마이그레이션, 시드 실행:

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify\server

npm run migrate
npm run seed
```

이후 매번 — 서버 실행:

```powershell
npm run dev
# → http://localhost:4000
```

---

**[터미널 3] 프론트엔드 실행**

```powershell
cd c:\redsky\work\react\peoplify_react\react-app-peoplify
npm run dev
# → http://localhost:5173
```

---

**브라우저 접속**

```
http://localhost:5173
```

### 종료

```powershell
docker-compose -f docker-compose.local.yml down
```

---

## 🔁 코드 수정 후 반영 방법

| 방법 | 프론트 변경 | 서버 변경 | DB 스키마 변경 |
|------|-----------|---------|--------------|
| **A (전체 Docker)** | `up --build` 재실행 | `up --build` 재실행 | `up --build` 재실행 |
| **B (하이브리드)** | 자동 반영 (핫리로드) | 자동 반영 (nodemon) | `npm run migrate` 재실행 |

---

## 🗄️ DB 직접 접속 (DBeaver 등)

| 항목 | 값 |
|------|----|
| Host | localhost |
| Port | 5432 |
| Database | peoplify |
| User | postgres |
| Password | peoplify |

---

## ❓ 자주 발생하는 문제

### Docker Desktop이 실행 중이지 않을 때

```
error during connect: ... Is the docker daemon running?
```

→ Docker Desktop 앱을 먼저 실행하고 트레이 아이콘이 초록색인지 확인

---

### 포트가 이미 사용 중일 때

```
Bind for 0.0.0.0:5432 failed: port is already allocated
```

→ 로컬에 PostgreSQL이 설치되어 있으면 충돌 발생. 로컬 PostgreSQL 서비스를 중지 후 재시도

```powershell
Stop-Service -Name postgresql*
```

---

### 로그인 시 401 오류 (방법 A)

→ 시드 데이터 미삽입. 볼륨 삭제 후 재빌드

```powershell
docker-compose -f docker-compose.local.yml down -v
docker-compose -f docker-compose.local.yml up --build
```

---

### 로그인 시 401 오류 (방법 B)

→ 시드 데이터 미실행. server 폴더에서 실행

```powershell
cd server
npm run seed
```



---------------------------------------------------------------------------------

# 로컬 실행
## docker desktop 설치 필수

* docker 실행
	- docker-compose -f docker-compose.local.yml up --build
* docker 종료
	- docker-compose -f docker-compose.local.yml down
* DB 데이터까지 완전 삭제하려면:
	- docker-compose -f docker-compose.local.yml down -v




## 프론트, server는 로컬에서 따로 띄우고 db만 docker로 띄우는 방법
* 이미 모든 설정이 갖춰져 있습니다! server/.env의 DB URL이 localhost:5432를 바라보고 있고, docker-compose도 5432 포트를 외부에 열어두고 있어서 바로 됩니다.

🏗️ 하이브리드 실행 방법 (DB만 Docker)
1단계 — DB만 Docker로 실행

docker-compose -f docker-compose.local.yml up db -d
db 서비스만 백그라운드로 실행. PostgreSQL이 localhost:5432로 접근 가능해집니다.

2단계 — 서버 초기화 (최초 1회만)

cd server

# 테이블 생성
npm run migrate

# 초기 데이터 삽입 (admin 계정 등)
npm run seed
3단계 — 서버 실행

# server 폴더에서
npm run dev
# → http://localhost:4000
4단계 — 프론트엔드 실행

# 루트 폴더에서 (새 터미널)
cd c:\redsky\work\react\peoplify_react\react-app-peoplify
npm run dev
# → http://localhost:5173
✅ 왜 바로 동작하는가?
각 설정이 이미 맞춰져 있습니다:

파일	설정	값
server/.env	DATABASE_URL	postgresql://postgres:peoplify@localhost:5432/peoplify
docker-compose.local.yml	DB 포트	"5432:5432" 외부 노출
.env	VITE_API_BASE_URL	http://localhost:4000
프론트(5173) → 서버(4000) → DB Docker(5432) 체인이 완성됩니다.

🗂️ 터미널 3개 구성 요약

터미널 1: docker-compose -f docker-compose.local.yml up db -d  (최초 1회)
터미널 2: cd server && npm run dev
터미널 3: npm run dev  (루트)
DB 종료할 때:
	docker-compose -f docker-compose.local.yml down

