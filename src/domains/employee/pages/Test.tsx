// [AUTO-GENERATED by scripts/synth-large-file.ts — 큰 파일 측정용 합성 픽스처]
import { useApi } from '@axiom/hooks';
import {
	Button,
	Card,
	CardContent,
	CardHeader,
	CardTitle,
	Input,
	Select,
	SelectContent,
	SelectItem,
	SelectTrigger,
	SelectValue,
} from '@axiom/components/ui';
import { useEffect, useState } from 'react';

/** 상태 옵션 공통 타입 */
type TStatusOption = { value: string; label: string };

// 부서관리 행 타입
type TDeptRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TDeptResponse = { success: boolean; data: TDeptRow[]; total: number };
const DEPT_ENDPOINT = '/api/dept';

// 직원관리 행 타입
type TEmployeeRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TEmployeeResponse = { success: boolean; data: TEmployeeRow[]; total: number };
const EMPLOYEE_ENDPOINT = '/api/employee';

// 프로젝트관리 행 타입
type TProjectRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TProjectResponse = { success: boolean; data: TProjectRow[]; total: number };
const PROJECT_ENDPOINT = '/api/project';

// 근태관리 행 타입
type TAttendanceRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TAttendanceResponse = { success: boolean; data: TAttendanceRow[]; total: number };
const ATTENDANCE_ENDPOINT = '/api/attendance';

// 휴가관리 행 타입
type TLeaveRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TLeaveResponse = { success: boolean; data: TLeaveRow[]; total: number };
const LEAVE_ENDPOINT = '/api/leave';

// 급여관리 행 타입
type TPayrollRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TPayrollResponse = { success: boolean; data: TPayrollRow[]; total: number };
const PAYROLL_ENDPOINT = '/api/payroll';

// 교육관리 행 타입
type TTrainingRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TTrainingResponse = { success: boolean; data: TTrainingRow[]; total: number };
const TRAINING_ENDPOINT = '/api/training';

// 자격증관리 행 타입
type TLicenseRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TLicenseResponse = { success: boolean; data: TLicenseRow[]; total: number };
const LICENSE_ENDPOINT = '/api/license';

// 평가관리 행 타입
type TEvaluationRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TEvaluationResponse = { success: boolean; data: TEvaluationRow[]; total: number };
const EVALUATION_ENDPOINT = '/api/evaluation';

// 조직관리 행 타입
type TOrgRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TOrgResponse = { success: boolean; data: TOrgRow[]; total: number };
const ORG_ENDPOINT = '/api/org';

// 발령관리 행 타입
type TAssignmentRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TAssignmentResponse = { success: boolean; data: TAssignmentRow[]; total: number };
const ASSIGNMENT_ENDPOINT = '/api/assignment';

// 출장관리 행 타입
type TTravelRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TTravelResponse = { success: boolean; data: TTravelRow[]; total: number };
const TRAVEL_ENDPOINT = '/api/travel';

// 경비관리 행 타입
type TExpenseRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TExpenseResponse = { success: boolean; data: TExpenseRow[]; total: number };
const EXPENSE_ENDPOINT = '/api/expense';

// 자산관리 행 타입
type TAssetRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TAssetResponse = { success: boolean; data: TAssetRow[]; total: number };
const ASSET_ENDPOINT = '/api/asset';

// 회의실관리 행 타입
type TRoomRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TRoomResponse = { success: boolean; data: TRoomRow[]; total: number };
const ROOM_ENDPOINT = '/api/room';

// 공지관리 행 타입
type TNoticeRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TNoticeResponse = { success: boolean; data: TNoticeRow[]; total: number };
const NOTICE_ENDPOINT = '/api/notice';

// 문서관리 행 타입
type TDocumentRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TDocumentResponse = { success: boolean; data: TDocumentRow[]; total: number };
const DOCUMENT_ENDPOINT = '/api/document';

// 결재관리 행 타입
type TApprovalRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TApprovalResponse = { success: boolean; data: TApprovalRow[]; total: number };
const APPROVAL_ENDPOINT = '/api/approval';

// 거래처관리 행 타입
type TClientRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TClientResponse = { success: boolean; data: TClientRow[]; total: number };
const CLIENT_ENDPOINT = '/api/client';

// 계약관리 행 타입
type TContractRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TContractResponse = { success: boolean; data: TContractRow[]; total: number };
const CONTRACT_ENDPOINT = '/api/contract';

// 매출관리 행 타입
type TSalesRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TSalesResponse = { success: boolean; data: TSalesRow[]; total: number };
const SALES_ENDPOINT = '/api/sales';

// 구매관리 행 타입
type TPurchaseRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TPurchaseResponse = { success: boolean; data: TPurchaseRow[]; total: number };
const PURCHASE_ENDPOINT = '/api/purchase';

// 재고관리 행 타입
type TInventoryRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TInventoryResponse = { success: boolean; data: TInventoryRow[]; total: number };
const INVENTORY_ENDPOINT = '/api/inventory';

// 품질관리 행 타입
type TQualityRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TQualityResponse = { success: boolean; data: TQualityRow[]; total: number };
const QUALITY_ENDPOINT = '/api/quality';

// 생산관리 행 타입
type TProductionRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TProductionResponse = { success: boolean; data: TProductionRow[]; total: number };
const PRODUCTION_ENDPOINT = '/api/production';

// 설비관리 행 타입
type TEquipmentRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TEquipmentResponse = { success: boolean; data: TEquipmentRow[]; total: number };
const EQUIPMENT_ENDPOINT = '/api/equipment';

// 안전관리 행 타입
type TSafetyRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TSafetyResponse = { success: boolean; data: TSafetyRow[]; total: number };
const SAFETY_ENDPOINT = '/api/safety';

// 민원관리 행 타입
type TComplaintRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TComplaintResponse = { success: boolean; data: TComplaintRow[]; total: number };
const COMPLAINT_ENDPOINT = '/api/complaint';

// 고객관리 행 타입
type TCustomerRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TCustomerResponse = { success: boolean; data: TCustomerRow[]; total: number };
const CUSTOMER_ENDPOINT = '/api/customer';

// 마케팅관리 행 타입
type TMarketingRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TMarketingResponse = { success: boolean; data: TMarketingRow[]; total: number };
const MARKETING_ENDPOINT = '/api/marketing';

// 영업관리 행 타입
type TBizRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TBizResponse = { success: boolean; data: TBizRow[]; total: number };
const BIZ_ENDPOINT = '/api/biz';

// 회계관리 행 타입
type TAccountingRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TAccountingResponse = { success: boolean; data: TAccountingRow[]; total: number };
const ACCOUNTING_ENDPOINT = '/api/accounting';

// 예산관리 행 타입
type TBudgetRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TBudgetResponse = { success: boolean; data: TBudgetRow[]; total: number };
const BUDGET_ENDPOINT = '/api/budget';

// 세무관리 행 타입
type TTaxRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TTaxResponse = { success: boolean; data: TTaxRow[]; total: number };
const TAX_ENDPOINT = '/api/tax';

// 감사관리 행 타입
type TAuditRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TAuditResponse = { success: boolean; data: TAuditRow[]; total: number };
const AUDIT_ENDPOINT = '/api/audit';

// 법무관리 행 타입
type TLegalRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TLegalResponse = { success: boolean; data: TLegalRow[]; total: number };
const LEGAL_ENDPOINT = '/api/legal';

// 보안관리 행 타입
type TSecurityRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TSecurityResponse = { success: boolean; data: TSecurityRow[]; total: number };
const SECURITY_ENDPOINT = '/api/security';

// 전산관리 행 타입
type TItRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TItResponse = { success: boolean; data: TItRow[]; total: number };
const IT_ENDPOINT = '/api/it';

// 네트워크관리 행 타입
type TNetworkRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TNetworkResponse = { success: boolean; data: TNetworkRow[]; total: number };
const NETWORK_ENDPOINT = '/api/network';

// 서버관리 행 타입
type TServerRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TServerResponse = { success: boolean; data: TServerRow[]; total: number };
const SERVER_ENDPOINT = '/api/server';

// 데이터관리 행 타입
type TDatasetRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TDatasetResponse = { success: boolean; data: TDatasetRow[]; total: number };
const DATASET_ENDPOINT = '/api/dataset';

// 백업관리 행 타입
type TBackupRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TBackupResponse = { success: boolean; data: TBackupRow[]; total: number };
const BACKUP_ENDPOINT = '/api/backup';

// 라이선스관리 행 타입
type TLicenseKeyRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TLicenseKeyResponse = { success: boolean; data: TLicenseKeyRow[]; total: number };
const LICENSEKEY_ENDPOINT = '/api/licensekey';

// 권한관리 행 타입
type TPermissionRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TPermissionResponse = { success: boolean; data: TPermissionRow[]; total: number };
const PERMISSION_ENDPOINT = '/api/permission';

// 메뉴관리 행 타입
type TMenuRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TMenuResponse = { success: boolean; data: TMenuRow[]; total: number };
const MENU_ENDPOINT = '/api/menu';

// 코드관리 행 타입
type TCommonCodeRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TCommonCodeResponse = { success: boolean; data: TCommonCodeRow[]; total: number };
const COMMONCODE_ENDPOINT = '/api/commoncode';

// 배치관리 행 타입
type TBatchRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TBatchResponse = { success: boolean; data: TBatchRow[]; total: number };
const BATCH_ENDPOINT = '/api/batch';

// 로그관리 행 타입
type TLogRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TLogResponse = { success: boolean; data: TLogRow[]; total: number };
const LOG_ENDPOINT = '/api/log';

// 알림관리 행 타입
type TAlertRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TAlertResponse = { success: boolean; data: TAlertRow[]; total: number };
const ALERT_ENDPOINT = '/api/alert';

// 메일관리 행 타입
type TMailRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TMailResponse = { success: boolean; data: TMailRow[]; total: number };
const MAIL_ENDPOINT = '/api/mail';

// 게시판관리 행 타입
type TBoardRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TBoardResponse = { success: boolean; data: TBoardRow[]; total: number };
const BOARD_ENDPOINT = '/api/board';

// 댓글관리 행 타입
type TCommentRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TCommentResponse = { success: boolean; data: TCommentRow[]; total: number };
const COMMENT_ENDPOINT = '/api/comment';

// 첨부관리 행 타입
type TAttachRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TAttachResponse = { success: boolean; data: TAttachRow[]; total: number };
const ATTACH_ENDPOINT = '/api/attach';

// 이미지관리 행 타입
type TImageRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TImageResponse = { success: boolean; data: TImageRow[]; total: number };
const IMAGE_ENDPOINT = '/api/image';

// 동영상관리 행 타입
type TVideoRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TVideoResponse = { success: boolean; data: TVideoRow[]; total: number };
const VIDEO_ENDPOINT = '/api/video';

// 통계관리 행 타입
type TStatRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TStatResponse = { success: boolean; data: TStatRow[]; total: number };
const STAT_ENDPOINT = '/api/stat';

// 리포트관리 행 타입
type TReportRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TReportResponse = { success: boolean; data: TReportRow[]; total: number };
const REPORT_ENDPOINT = '/api/report';

// 대시보드관리 행 타입
type TDashboardRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TDashboardResponse = { success: boolean; data: TDashboardRow[]; total: number };
const DASHBOARD_ENDPOINT = '/api/dashboard';

// 캘린더관리 행 타입
type TCalendarRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TCalendarResponse = { success: boolean; data: TCalendarRow[]; total: number };
const CALENDAR_ENDPOINT = '/api/calendar';

// 일정관리 행 타입
type TScheduleRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TScheduleResponse = { success: boolean; data: TScheduleRow[]; total: number };
const SCHEDULE_ENDPOINT = '/api/schedule';

// 연락처관리 행 타입
type TPhoneRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TPhoneResponse = { success: boolean; data: TPhoneRow[]; total: number };
const PHONE_ENDPOINT = '/api/phone';

// 주소록관리 행 타입
type TAddrbookRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TAddrbookResponse = { success: boolean; data: TAddrbookRow[]; total: number };
const ADDRBOOK_ENDPOINT = '/api/addrbook';

// 즐겨찾기관리 행 타입
type TFavoriteRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TFavoriteResponse = { success: boolean; data: TFavoriteRow[]; total: number };
const FAVORITE_ENDPOINT = '/api/favorite';

// 설정관리 행 타입
type TSettingRow = {
	id: number;
	name: string;
	status: 'active' | 'pending' | 'done';
	owner: string;
	amount: number;
	updated_at: string;
};
type TSettingResponse = { success: boolean; data: TSettingRow[]; total: number };
const SETTING_ENDPOINT = '/api/setting';

export default function MegaDashboardPage(): React.ReactNode {
	/** 부서관리 — 선택 상태 + 서버 조회 */
	const [selectedDept, setSelectedDept] = useState<string>('all');
	const [searchDept, setSearchDept] = useState<string>('');
	const { data: deptResp, isPending: deptPending } = useApi<TDeptResponse>(DEPT_ENDPOINT, {
		params: { status: selectedDept === 'all' ? undefined : selectedDept, q: searchDept || undefined },
	});
	const deptRows = deptResp?.data ?? [];
	const deptStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 직원관리 — 선택 상태 + 서버 조회 */
	const [selectedEmployee, setSelectedEmployee] = useState<string>('all');
	const [searchEmployee, setSearchEmployee] = useState<string>('');
	const { data: employeeResp, isPending: employeePending } = useApi<TEmployeeResponse>(EMPLOYEE_ENDPOINT, {
		params: { status: selectedEmployee === 'all' ? undefined : selectedEmployee, q: searchEmployee || undefined },
	});
	const employeeRows = employeeResp?.data ?? [];
	const employeeStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 프로젝트관리 — 선택 상태 + 서버 조회 */
	const [selectedProject, setSelectedProject] = useState<string>('all');
	const [searchProject, setSearchProject] = useState<string>('');
	const { data: projectResp, isPending: projectPending } = useApi<TProjectResponse>(PROJECT_ENDPOINT, {
		params: { status: selectedProject === 'all' ? undefined : selectedProject, q: searchProject || undefined },
	});
	const projectRows = projectResp?.data ?? [];
	const projectStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 근태관리 — 선택 상태 + 서버 조회 */
	const [selectedAttendance, setSelectedAttendance] = useState<string>('all');
	const [searchAttendance, setSearchAttendance] = useState<string>('');
	const { data: attendanceResp, isPending: attendancePending } = useApi<TAttendanceResponse>(ATTENDANCE_ENDPOINT, {
		params: { status: selectedAttendance === 'all' ? undefined : selectedAttendance, q: searchAttendance || undefined },
	});
	const attendanceRows = attendanceResp?.data ?? [];
	const attendanceStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 휴가관리 — 선택 상태 + 서버 조회 */
	const [selectedLeave, setSelectedLeave] = useState<string>('all');
	const [searchLeave, setSearchLeave] = useState<string>('');
	const { data: leaveResp, isPending: leavePending } = useApi<TLeaveResponse>(LEAVE_ENDPOINT, {
		params: { status: selectedLeave === 'all' ? undefined : selectedLeave, q: searchLeave || undefined },
	});
	const leaveRows = leaveResp?.data ?? [];
	const leaveStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 급여관리 — 선택 상태 + 서버 조회 */
	const [selectedPayroll, setSelectedPayroll] = useState<string>('all');
	const [searchPayroll, setSearchPayroll] = useState<string>('');
	const { data: payrollResp, isPending: payrollPending } = useApi<TPayrollResponse>(PAYROLL_ENDPOINT, {
		params: { status: selectedPayroll === 'all' ? undefined : selectedPayroll, q: searchPayroll || undefined },
	});
	const payrollRows = payrollResp?.data ?? [];
	const payrollStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 교육관리 — 선택 상태 + 서버 조회 */
	const [selectedTraining, setSelectedTraining] = useState<string>('all');
	const [searchTraining, setSearchTraining] = useState<string>('');
	const { data: trainingResp, isPending: trainingPending } = useApi<TTrainingResponse>(TRAINING_ENDPOINT, {
		params: { status: selectedTraining === 'all' ? undefined : selectedTraining, q: searchTraining || undefined },
	});
	const trainingRows = trainingResp?.data ?? [];
	const trainingStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 자격증관리 — 선택 상태 + 서버 조회 */
	const [selectedLicense, setSelectedLicense] = useState<string>('all');
	const [searchLicense, setSearchLicense] = useState<string>('');
	const { data: licenseResp, isPending: licensePending } = useApi<TLicenseResponse>(LICENSE_ENDPOINT, {
		params: { status: selectedLicense === 'all' ? undefined : selectedLicense, q: searchLicense || undefined },
	});
	const licenseRows = licenseResp?.data ?? [];
	const licenseStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 평가관리 — 선택 상태 + 서버 조회 */
	const [selectedEvaluation, setSelectedEvaluation] = useState<string>('all');
	const [searchEvaluation, setSearchEvaluation] = useState<string>('');
	const { data: evaluationResp, isPending: evaluationPending } = useApi<TEvaluationResponse>(EVALUATION_ENDPOINT, {
		params: { status: selectedEvaluation === 'all' ? undefined : selectedEvaluation, q: searchEvaluation || undefined },
	});
	const evaluationRows = evaluationResp?.data ?? [];
	const evaluationStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 조직관리 — 선택 상태 + 서버 조회 */
	const [selectedOrg, setSelectedOrg] = useState<string>('all');
	const [searchOrg, setSearchOrg] = useState<string>('');
	const { data: orgResp, isPending: orgPending } = useApi<TOrgResponse>(ORG_ENDPOINT, {
		params: { status: selectedOrg === 'all' ? undefined : selectedOrg, q: searchOrg || undefined },
	});
	const orgRows = orgResp?.data ?? [];
	const orgStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 발령관리 — 선택 상태 + 서버 조회 */
	const [selectedAssignment, setSelectedAssignment] = useState<string>('all');
	const [searchAssignment, setSearchAssignment] = useState<string>('');
	const { data: assignmentResp, isPending: assignmentPending } = useApi<TAssignmentResponse>(ASSIGNMENT_ENDPOINT, {
		params: { status: selectedAssignment === 'all' ? undefined : selectedAssignment, q: searchAssignment || undefined },
	});
	const assignmentRows = assignmentResp?.data ?? [];
	const assignmentStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 출장관리 — 선택 상태 + 서버 조회 */
	const [selectedTravel, setSelectedTravel] = useState<string>('all');
	const [searchTravel, setSearchTravel] = useState<string>('');
	const { data: travelResp, isPending: travelPending } = useApi<TTravelResponse>(TRAVEL_ENDPOINT, {
		params: { status: selectedTravel === 'all' ? undefined : selectedTravel, q: searchTravel || undefined },
	});
	const travelRows = travelResp?.data ?? [];
	const travelStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 경비관리 — 선택 상태 + 서버 조회 */
	const [selectedExpense, setSelectedExpense] = useState<string>('all');
	const [searchExpense, setSearchExpense] = useState<string>('');
	const { data: expenseResp, isPending: expensePending } = useApi<TExpenseResponse>(EXPENSE_ENDPOINT, {
		params: { status: selectedExpense === 'all' ? undefined : selectedExpense, q: searchExpense || undefined },
	});
	const expenseRows = expenseResp?.data ?? [];
	const expenseStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 자산관리 — 선택 상태 + 서버 조회 */
	const [selectedAsset, setSelectedAsset] = useState<string>('all');
	const [searchAsset, setSearchAsset] = useState<string>('');
	const { data: assetResp, isPending: assetPending } = useApi<TAssetResponse>(ASSET_ENDPOINT, {
		params: { status: selectedAsset === 'all' ? undefined : selectedAsset, q: searchAsset || undefined },
	});
	const assetRows = assetResp?.data ?? [];
	const assetStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 회의실관리 — 선택 상태 + 서버 조회 */
	const [selectedRoom, setSelectedRoom] = useState<string>('all');
	const [searchRoom, setSearchRoom] = useState<string>('');
	const { data: roomResp, isPending: roomPending } = useApi<TRoomResponse>(ROOM_ENDPOINT, {
		params: { status: selectedRoom === 'all' ? undefined : selectedRoom, q: searchRoom || undefined },
	});
	const roomRows = roomResp?.data ?? [];
	const roomStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 공지관리 — 선택 상태 + 서버 조회 */
	const [selectedNotice, setSelectedNotice] = useState<string>('all');
	const [searchNotice, setSearchNotice] = useState<string>('');
	const { data: noticeResp, isPending: noticePending } = useApi<TNoticeResponse>(NOTICE_ENDPOINT, {
		params: { status: selectedNotice === 'all' ? undefined : selectedNotice, q: searchNotice || undefined },
	});
	const noticeRows = noticeResp?.data ?? [];
	const noticeStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 문서관리 — 선택 상태 + 서버 조회 */
	const [selectedDocument, setSelectedDocument] = useState<string>('all');
	const [searchDocument, setSearchDocument] = useState<string>('');
	const { data: documentResp, isPending: documentPending } = useApi<TDocumentResponse>(DOCUMENT_ENDPOINT, {
		params: { status: selectedDocument === 'all' ? undefined : selectedDocument, q: searchDocument || undefined },
	});
	const documentRows = documentResp?.data ?? [];
	const documentStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 결재관리 — 선택 상태 + 서버 조회 */
	const [selectedApproval, setSelectedApproval] = useState<string>('all');
	const [searchApproval, setSearchApproval] = useState<string>('');
	const { data: approvalResp, isPending: approvalPending } = useApi<TApprovalResponse>(APPROVAL_ENDPOINT, {
		params: { status: selectedApproval === 'all' ? undefined : selectedApproval, q: searchApproval || undefined },
	});
	const approvalRows = approvalResp?.data ?? [];
	const approvalStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 거래처관리 — 선택 상태 + 서버 조회 */
	const [selectedClient, setSelectedClient] = useState<string>('all');
	const [searchClient, setSearchClient] = useState<string>('');
	const { data: clientResp, isPending: clientPending } = useApi<TClientResponse>(CLIENT_ENDPOINT, {
		params: { status: selectedClient === 'all' ? undefined : selectedClient, q: searchClient || undefined },
	});
	const clientRows = clientResp?.data ?? [];
	const clientStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 계약관리 — 선택 상태 + 서버 조회 */
	const [selectedContract, setSelectedContract] = useState<string>('all');
	const [searchContract, setSearchContract] = useState<string>('');
	const { data: contractResp, isPending: contractPending } = useApi<TContractResponse>(CONTRACT_ENDPOINT, {
		params: { status: selectedContract === 'all' ? undefined : selectedContract, q: searchContract || undefined },
	});
	const contractRows = contractResp?.data ?? [];
	const contractStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 매출관리 — 선택 상태 + 서버 조회 */
	const [selectedSales, setSelectedSales] = useState<string>('all');
	const [searchSales, setSearchSales] = useState<string>('');
	const { data: salesResp, isPending: salesPending } = useApi<TSalesResponse>(SALES_ENDPOINT, {
		params: { status: selectedSales === 'all' ? undefined : selectedSales, q: searchSales || undefined },
	});
	const salesRows = salesResp?.data ?? [];
	const salesStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 구매관리 — 선택 상태 + 서버 조회 */
	const [selectedPurchase, setSelectedPurchase] = useState<string>('all');
	const [searchPurchase, setSearchPurchase] = useState<string>('');
	const { data: purchaseResp, isPending: purchasePending } = useApi<TPurchaseResponse>(PURCHASE_ENDPOINT, {
		params: { status: selectedPurchase === 'all' ? undefined : selectedPurchase, q: searchPurchase || undefined },
	});
	const purchaseRows = purchaseResp?.data ?? [];
	const purchaseStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 재고관리 — 선택 상태 + 서버 조회 */
	const [selectedInventory, setSelectedInventory] = useState<string>('all');
	const [searchInventory, setSearchInventory] = useState<string>('');
	const { data: inventoryResp, isPending: inventoryPending } = useApi<TInventoryResponse>(INVENTORY_ENDPOINT, {
		params: { status: selectedInventory === 'all' ? undefined : selectedInventory, q: searchInventory || undefined },
	});
	const inventoryRows = inventoryResp?.data ?? [];
	const inventoryStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 품질관리 — 선택 상태 + 서버 조회 */
	const [selectedQuality, setSelectedQuality] = useState<string>('all');
	const [searchQuality, setSearchQuality] = useState<string>('');
	const { data: qualityResp, isPending: qualityPending } = useApi<TQualityResponse>(QUALITY_ENDPOINT, {
		params: { status: selectedQuality === 'all' ? undefined : selectedQuality, q: searchQuality || undefined },
	});
	const qualityRows = qualityResp?.data ?? [];
	const qualityStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 생산관리 — 선택 상태 + 서버 조회 */
	const [selectedProduction, setSelectedProduction] = useState<string>('all');
	const [searchProduction, setSearchProduction] = useState<string>('');
	const { data: productionResp, isPending: productionPending } = useApi<TProductionResponse>(PRODUCTION_ENDPOINT, {
		params: { status: selectedProduction === 'all' ? undefined : selectedProduction, q: searchProduction || undefined },
	});
	const productionRows = productionResp?.data ?? [];
	const productionStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 설비관리 — 선택 상태 + 서버 조회 */
	const [selectedEquipment, setSelectedEquipment] = useState<string>('all');
	const [searchEquipment, setSearchEquipment] = useState<string>('');
	const { data: equipmentResp, isPending: equipmentPending } = useApi<TEquipmentResponse>(EQUIPMENT_ENDPOINT, {
		params: { status: selectedEquipment === 'all' ? undefined : selectedEquipment, q: searchEquipment || undefined },
	});
	const equipmentRows = equipmentResp?.data ?? [];
	const equipmentStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 안전관리 — 선택 상태 + 서버 조회 */
	const [selectedSafety, setSelectedSafety] = useState<string>('all');
	const [searchSafety, setSearchSafety] = useState<string>('');
	const { data: safetyResp, isPending: safetyPending } = useApi<TSafetyResponse>(SAFETY_ENDPOINT, {
		params: { status: selectedSafety === 'all' ? undefined : selectedSafety, q: searchSafety || undefined },
	});
	const safetyRows = safetyResp?.data ?? [];
	const safetyStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 민원관리 — 선택 상태 + 서버 조회 */
	const [selectedComplaint, setSelectedComplaint] = useState<string>('all');
	const [searchComplaint, setSearchComplaint] = useState<string>('');
	const { data: complaintResp, isPending: complaintPending } = useApi<TComplaintResponse>(COMPLAINT_ENDPOINT, {
		params: { status: selectedComplaint === 'all' ? undefined : selectedComplaint, q: searchComplaint || undefined },
	});
	const complaintRows = complaintResp?.data ?? [];
	const complaintStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 고객관리 — 선택 상태 + 서버 조회 */
	const [selectedCustomer, setSelectedCustomer] = useState<string>('all');
	const [searchCustomer, setSearchCustomer] = useState<string>('');
	const { data: customerResp, isPending: customerPending } = useApi<TCustomerResponse>(CUSTOMER_ENDPOINT, {
		params: { status: selectedCustomer === 'all' ? undefined : selectedCustomer, q: searchCustomer || undefined },
	});
	const customerRows = customerResp?.data ?? [];
	const customerStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 마케팅관리 — 선택 상태 + 서버 조회 */
	const [selectedMarketing, setSelectedMarketing] = useState<string>('all');
	const [searchMarketing, setSearchMarketing] = useState<string>('');
	const { data: marketingResp, isPending: marketingPending } = useApi<TMarketingResponse>(MARKETING_ENDPOINT, {
		params: { status: selectedMarketing === 'all' ? undefined : selectedMarketing, q: searchMarketing || undefined },
	});
	const marketingRows = marketingResp?.data ?? [];
	const marketingStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 영업관리 — 선택 상태 + 서버 조회 */
	const [selectedBiz, setSelectedBiz] = useState<string>('all');
	const [searchBiz, setSearchBiz] = useState<string>('');
	const { data: bizResp, isPending: bizPending } = useApi<TBizResponse>(BIZ_ENDPOINT, {
		params: { status: selectedBiz === 'all' ? undefined : selectedBiz, q: searchBiz || undefined },
	});
	const bizRows = bizResp?.data ?? [];
	const bizStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 회계관리 — 선택 상태 + 서버 조회 */
	const [selectedAccounting, setSelectedAccounting] = useState<string>('all');
	const [searchAccounting, setSearchAccounting] = useState<string>('');
	const { data: accountingResp, isPending: accountingPending } = useApi<TAccountingResponse>(ACCOUNTING_ENDPOINT, {
		params: { status: selectedAccounting === 'all' ? undefined : selectedAccounting, q: searchAccounting || undefined },
	});
	const accountingRows = accountingResp?.data ?? [];
	const accountingStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 예산관리 — 선택 상태 + 서버 조회 */
	const [selectedBudget, setSelectedBudget] = useState<string>('all');
	const [searchBudget, setSearchBudget] = useState<string>('');
	const { data: budgetResp, isPending: budgetPending } = useApi<TBudgetResponse>(BUDGET_ENDPOINT, {
		params: { status: selectedBudget === 'all' ? undefined : selectedBudget, q: searchBudget || undefined },
	});
	const budgetRows = budgetResp?.data ?? [];
	const budgetStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 세무관리 — 선택 상태 + 서버 조회 */
	const [selectedTax, setSelectedTax] = useState<string>('all');
	const [searchTax, setSearchTax] = useState<string>('');
	const { data: taxResp, isPending: taxPending } = useApi<TTaxResponse>(TAX_ENDPOINT, {
		params: { status: selectedTax === 'all' ? undefined : selectedTax, q: searchTax || undefined },
	});
	const taxRows = taxResp?.data ?? [];
	const taxStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 감사관리 — 선택 상태 + 서버 조회 */
	const [selectedAudit, setSelectedAudit] = useState<string>('all');
	const [searchAudit, setSearchAudit] = useState<string>('');
	const { data: auditResp, isPending: auditPending } = useApi<TAuditResponse>(AUDIT_ENDPOINT, {
		params: { status: selectedAudit === 'all' ? undefined : selectedAudit, q: searchAudit || undefined },
	});
	const auditRows = auditResp?.data ?? [];
	const auditStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 법무관리 — 선택 상태 + 서버 조회 */
	const [selectedLegal, setSelectedLegal] = useState<string>('all');
	const [searchLegal, setSearchLegal] = useState<string>('');
	const { data: legalResp, isPending: legalPending } = useApi<TLegalResponse>(LEGAL_ENDPOINT, {
		params: { status: selectedLegal === 'all' ? undefined : selectedLegal, q: searchLegal || undefined },
	});
	const legalRows = legalResp?.data ?? [];
	const legalStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 보안관리 — 선택 상태 + 서버 조회 */
	const [selectedSecurity, setSelectedSecurity] = useState<string>('all');
	const [searchSecurity, setSearchSecurity] = useState<string>('');
	const { data: securityResp, isPending: securityPending } = useApi<TSecurityResponse>(SECURITY_ENDPOINT, {
		params: { status: selectedSecurity === 'all' ? undefined : selectedSecurity, q: searchSecurity || undefined },
	});
	const securityRows = securityResp?.data ?? [];
	const securityStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 전산관리 — 선택 상태 + 서버 조회 */
	const [selectedIt, setSelectedIt] = useState<string>('all');
	const [searchIt, setSearchIt] = useState<string>('');
	const { data: itResp, isPending: itPending } = useApi<TItResponse>(IT_ENDPOINT, {
		params: { status: selectedIt === 'all' ? undefined : selectedIt, q: searchIt || undefined },
	});
	const itRows = itResp?.data ?? [];
	const itStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 네트워크관리 — 선택 상태 + 서버 조회 */
	const [selectedNetwork, setSelectedNetwork] = useState<string>('all');
	const [searchNetwork, setSearchNetwork] = useState<string>('');
	const { data: networkResp, isPending: networkPending } = useApi<TNetworkResponse>(NETWORK_ENDPOINT, {
		params: { status: selectedNetwork === 'all' ? undefined : selectedNetwork, q: searchNetwork || undefined },
	});
	const networkRows = networkResp?.data ?? [];
	const networkStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 서버관리 — 선택 상태 + 서버 조회 */
	const [selectedServer, setSelectedServer] = useState<string>('all');
	const [searchServer, setSearchServer] = useState<string>('');
	const { data: serverResp, isPending: serverPending } = useApi<TServerResponse>(SERVER_ENDPOINT, {
		params: { status: selectedServer === 'all' ? undefined : selectedServer, q: searchServer || undefined },
	});
	const serverRows = serverResp?.data ?? [];
	const serverStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 데이터관리 — 선택 상태 + 서버 조회 */
	const [selectedDataset, setSelectedDataset] = useState<string>('all');
	const [searchDataset, setSearchDataset] = useState<string>('');
	const { data: datasetResp, isPending: datasetPending } = useApi<TDatasetResponse>(DATASET_ENDPOINT, {
		params: { status: selectedDataset === 'all' ? undefined : selectedDataset, q: searchDataset || undefined },
	});
	const datasetRows = datasetResp?.data ?? [];
	const datasetStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 백업관리 — 선택 상태 + 서버 조회 */
	const [selectedBackup, setSelectedBackup] = useState<string>('all');
	const [searchBackup, setSearchBackup] = useState<string>('');
	const { data: backupResp, isPending: backupPending } = useApi<TBackupResponse>(BACKUP_ENDPOINT, {
		params: { status: selectedBackup === 'all' ? undefined : selectedBackup, q: searchBackup || undefined },
	});
	const backupRows = backupResp?.data ?? [];
	const backupStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 라이선스관리 — 선택 상태 + 서버 조회 */
	const [selectedLicenseKey, setSelectedLicenseKey] = useState<string>('all');
	const [searchLicenseKey, setSearchLicenseKey] = useState<string>('');
	const { data: licenseKeyResp, isPending: licenseKeyPending } = useApi<TLicenseKeyResponse>(LICENSEKEY_ENDPOINT, {
		params: { status: selectedLicenseKey === 'all' ? undefined : selectedLicenseKey, q: searchLicenseKey || undefined },
	});
	const licenseKeyRows = licenseKeyResp?.data ?? [];
	const licenseKeyStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 권한관리 — 선택 상태 + 서버 조회 */
	const [selectedPermission, setSelectedPermission] = useState<string>('all');
	const [searchPermission, setSearchPermission] = useState<string>('');
	const { data: permissionResp, isPending: permissionPending } = useApi<TPermissionResponse>(PERMISSION_ENDPOINT, {
		params: { status: selectedPermission === 'all' ? undefined : selectedPermission, q: searchPermission || undefined },
	});
	const permissionRows = permissionResp?.data ?? [];
	const permissionStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 메뉴관리 — 선택 상태 + 서버 조회 */
	const [selectedMenu, setSelectedMenu] = useState<string>('all');
	const [searchMenu, setSearchMenu] = useState<string>('');
	const { data: menuResp, isPending: menuPending } = useApi<TMenuResponse>(MENU_ENDPOINT, {
		params: { status: selectedMenu === 'all' ? undefined : selectedMenu, q: searchMenu || undefined },
	});
	const menuRows = menuResp?.data ?? [];
	const menuStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 코드관리 — 선택 상태 + 서버 조회 */
	const [selectedCommonCode, setSelectedCommonCode] = useState<string>('all');
	const [searchCommonCode, setSearchCommonCode] = useState<string>('');
	const { data: commonCodeResp, isPending: commonCodePending } = useApi<TCommonCodeResponse>(COMMONCODE_ENDPOINT, {
		params: { status: selectedCommonCode === 'all' ? undefined : selectedCommonCode, q: searchCommonCode || undefined },
	});
	const commonCodeRows = commonCodeResp?.data ?? [];
	const commonCodeStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 배치관리 — 선택 상태 + 서버 조회 */
	const [selectedBatch, setSelectedBatch] = useState<string>('all');
	const [searchBatch, setSearchBatch] = useState<string>('');
	const { data: batchResp, isPending: batchPending } = useApi<TBatchResponse>(BATCH_ENDPOINT, {
		params: { status: selectedBatch === 'all' ? undefined : selectedBatch, q: searchBatch || undefined },
	});
	const batchRows = batchResp?.data ?? [];
	const batchStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 로그관리 — 선택 상태 + 서버 조회 */
	const [selectedLog, setSelectedLog] = useState<string>('all');
	const [searchLog, setSearchLog] = useState<string>('');
	const { data: logResp, isPending: logPending } = useApi<TLogResponse>(LOG_ENDPOINT, {
		params: { status: selectedLog === 'all' ? undefined : selectedLog, q: searchLog || undefined },
	});
	const logRows = logResp?.data ?? [];
	const logStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 알림관리 — 선택 상태 + 서버 조회 */
	const [selectedAlert, setSelectedAlert] = useState<string>('all');
	const [searchAlert, setSearchAlert] = useState<string>('');
	const { data: alertResp, isPending: alertPending } = useApi<TAlertResponse>(ALERT_ENDPOINT, {
		params: { status: selectedAlert === 'all' ? undefined : selectedAlert, q: searchAlert || undefined },
	});
	const alertRows = alertResp?.data ?? [];
	const alertStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 메일관리 — 선택 상태 + 서버 조회 */
	const [selectedMail, setSelectedMail] = useState<string>('all');
	const [searchMail, setSearchMail] = useState<string>('');
	const { data: mailResp, isPending: mailPending } = useApi<TMailResponse>(MAIL_ENDPOINT, {
		params: { status: selectedMail === 'all' ? undefined : selectedMail, q: searchMail || undefined },
	});
	const mailRows = mailResp?.data ?? [];
	const mailStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 게시판관리 — 선택 상태 + 서버 조회 */
	const [selectedBoard, setSelectedBoard] = useState<string>('all');
	const [searchBoard, setSearchBoard] = useState<string>('');
	const { data: boardResp, isPending: boardPending } = useApi<TBoardResponse>(BOARD_ENDPOINT, {
		params: { status: selectedBoard === 'all' ? undefined : selectedBoard, q: searchBoard || undefined },
	});
	const boardRows = boardResp?.data ?? [];
	const boardStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 댓글관리 — 선택 상태 + 서버 조회 */
	const [selectedComment, setSelectedComment] = useState<string>('all');
	const [searchComment, setSearchComment] = useState<string>('');
	const { data: commentResp, isPending: commentPending } = useApi<TCommentResponse>(COMMENT_ENDPOINT, {
		params: { status: selectedComment === 'all' ? undefined : selectedComment, q: searchComment || undefined },
	});
	const commentRows = commentResp?.data ?? [];
	const commentStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 첨부관리 — 선택 상태 + 서버 조회 */
	const [selectedAttach, setSelectedAttach] = useState<string>('all');
	const [searchAttach, setSearchAttach] = useState<string>('');
	const { data: attachResp, isPending: attachPending } = useApi<TAttachResponse>(ATTACH_ENDPOINT, {
		params: { status: selectedAttach === 'all' ? undefined : selectedAttach, q: searchAttach || undefined },
	});
	const attachRows = attachResp?.data ?? [];
	const attachStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 이미지관리 — 선택 상태 + 서버 조회 */
	const [selectedImage, setSelectedImage] = useState<string>('all');
	const [searchImage, setSearchImage] = useState<string>('');
	const { data: imageResp, isPending: imagePending } = useApi<TImageResponse>(IMAGE_ENDPOINT, {
		params: { status: selectedImage === 'all' ? undefined : selectedImage, q: searchImage || undefined },
	});
	const imageRows = imageResp?.data ?? [];
	const imageStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 동영상관리 — 선택 상태 + 서버 조회 */
	const [selectedVideo, setSelectedVideo] = useState<string>('all');
	const [searchVideo, setSearchVideo] = useState<string>('');
	const { data: videoResp, isPending: videoPending } = useApi<TVideoResponse>(VIDEO_ENDPOINT, {
		params: { status: selectedVideo === 'all' ? undefined : selectedVideo, q: searchVideo || undefined },
	});
	const videoRows = videoResp?.data ?? [];
	const videoStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 통계관리 — 선택 상태 + 서버 조회 */
	const [selectedStat, setSelectedStat] = useState<string>('all');
	const [searchStat, setSearchStat] = useState<string>('');
	const { data: statResp, isPending: statPending } = useApi<TStatResponse>(STAT_ENDPOINT, {
		params: { status: selectedStat === 'all' ? undefined : selectedStat, q: searchStat || undefined },
	});
	const statRows = statResp?.data ?? [];
	const statStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 리포트관리 — 선택 상태 + 서버 조회 */
	const [selectedReport, setSelectedReport] = useState<string>('all');
	const [searchReport, setSearchReport] = useState<string>('');
	const { data: reportResp, isPending: reportPending } = useApi<TReportResponse>(REPORT_ENDPOINT, {
		params: { status: selectedReport === 'all' ? undefined : selectedReport, q: searchReport || undefined },
	});
	const reportRows = reportResp?.data ?? [];
	const reportStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 대시보드관리 — 선택 상태 + 서버 조회 */
	const [selectedDashboard, setSelectedDashboard] = useState<string>('all');
	const [searchDashboard, setSearchDashboard] = useState<string>('');
	const { data: dashboardResp, isPending: dashboardPending } = useApi<TDashboardResponse>(DASHBOARD_ENDPOINT, {
		params: { status: selectedDashboard === 'all' ? undefined : selectedDashboard, q: searchDashboard || undefined },
	});
	const dashboardRows = dashboardResp?.data ?? [];
	const dashboardStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 캘린더관리 — 선택 상태 + 서버 조회 */
	const [selectedCalendar, setSelectedCalendar] = useState<string>('all');
	const [searchCalendar, setSearchCalendar] = useState<string>('');
	const { data: calendarResp, isPending: calendarPending } = useApi<TCalendarResponse>(CALENDAR_ENDPOINT, {
		params: { status: selectedCalendar === 'all' ? undefined : selectedCalendar, q: searchCalendar || undefined },
	});
	const calendarRows = calendarResp?.data ?? [];
	const calendarStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 일정관리 — 선택 상태 + 서버 조회 */
	const [selectedSchedule, setSelectedSchedule] = useState<string>('all');
	const [searchSchedule, setSearchSchedule] = useState<string>('');
	const { data: scheduleResp, isPending: schedulePending } = useApi<TScheduleResponse>(SCHEDULE_ENDPOINT, {
		params: { status: selectedSchedule === 'all' ? undefined : selectedSchedule, q: searchSchedule || undefined },
	});
	const scheduleRows = scheduleResp?.data ?? [];
	const scheduleStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 연락처관리 — 선택 상태 + 서버 조회 */
	const [selectedPhone, setSelectedPhone] = useState<string>('all');
	const [searchPhone, setSearchPhone] = useState<string>('');
	const { data: phoneResp, isPending: phonePending } = useApi<TPhoneResponse>(PHONE_ENDPOINT, {
		params: { status: selectedPhone === 'all' ? undefined : selectedPhone, q: searchPhone || undefined },
	});
	const phoneRows = phoneResp?.data ?? [];
	const phoneStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 주소록관리 — 선택 상태 + 서버 조회 */
	const [selectedAddrbook, setSelectedAddrbook] = useState<string>('all');
	const [searchAddrbook, setSearchAddrbook] = useState<string>('');
	const { data: addrbookResp, isPending: addrbookPending } = useApi<TAddrbookResponse>(ADDRBOOK_ENDPOINT, {
		params: { status: selectedAddrbook === 'all' ? undefined : selectedAddrbook, q: searchAddrbook || undefined },
	});
	const addrbookRows = addrbookResp?.data ?? [];
	const addrbookStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 즐겨찾기관리 — 선택 상태 + 서버 조회 */
	const [selectedFavorite, setSelectedFavorite] = useState<string>('all');
	const [searchFavorite, setSearchFavorite] = useState<string>('');
	const { data: favoriteResp, isPending: favoritePending } = useApi<TFavoriteResponse>(FAVORITE_ENDPOINT, {
		params: { status: selectedFavorite === 'all' ? undefined : selectedFavorite, q: searchFavorite || undefined },
	});
	const favoriteRows = favoriteResp?.data ?? [];
	const favoriteStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	/** 설정관리 — 선택 상태 + 서버 조회 */
	const [selectedSetting, setSelectedSetting] = useState<string>('all');
	const [searchSetting, setSearchSetting] = useState<string>('');
	const { data: settingResp, isPending: settingPending } = useApi<TSettingResponse>(SETTING_ENDPOINT, {
		params: { status: selectedSetting === 'all' ? undefined : selectedSetting, q: searchSetting || undefined },
	});
	const settingRows = settingResp?.data ?? [];
	const settingStatusOptions: TStatusOption[] = [
		{ value: 'all', label: '전체' },
		{ value: 'active', label: '진행' },
		{ value: 'pending', label: '대기' },
		{ value: 'done', label: '완료' },
	];

	return (
		<div className="p-5 space-y-6">
			{/* ===== 섹션 01: 부서관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>부서관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedDept}
							onValueChange={setSelectedDept}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="부서 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{deptStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchDept}
							onChange={(e) => setSearchDept(e.target.value)}
							placeholder="부서 검색..."
						/>
						<Button size="sm">부서 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{deptRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{deptPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>부서 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">부서 전체</div>
							<div className="text-2xl font-semibold">{deptRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">부서 진행</div>
							<div className="text-2xl font-semibold">{deptRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">부서 대기</div>
							<div className="text-2xl font-semibold">{deptRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">부서 완료</div>
							<div className="text-2xl font-semibold">{deptRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>부서 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 담당부서</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 책임자</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 등록일</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 수정일</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 상태코드</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 비고</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 연락처</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 분류</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 우선순위</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 진척률</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 예산코드</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">부서 승인자</dt>
							<dd>{deptRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 02: 직원관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>직원관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedEmployee}
							onValueChange={setSelectedEmployee}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="직원 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{employeeStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchEmployee}
							onChange={(e) => setSearchEmployee(e.target.value)}
							placeholder="직원 검색..."
						/>
						<Button size="sm">직원 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{employeeRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{employeePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>직원 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">직원 전체</div>
							<div className="text-2xl font-semibold">{employeeRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">직원 진행</div>
							<div className="text-2xl font-semibold">{employeeRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">직원 대기</div>
							<div className="text-2xl font-semibold">{employeeRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">직원 완료</div>
							<div className="text-2xl font-semibold">{employeeRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>직원 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 담당부서</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 책임자</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 등록일</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 수정일</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 상태코드</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 비고</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 연락처</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 분류</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 우선순위</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 진척률</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 예산코드</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">직원 승인자</dt>
							<dd>{employeeRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 03: 프로젝트관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>프로젝트관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedProject}
							onValueChange={setSelectedProject}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="프로젝트 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{projectStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchProject}
							onChange={(e) => setSearchProject(e.target.value)}
							placeholder="프로젝트 검색..."
						/>
						<Button size="sm">프로젝트 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{projectRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{projectPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>프로젝트 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">프로젝트 전체</div>
							<div className="text-2xl font-semibold">{projectRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">프로젝트 진행</div>
							<div className="text-2xl font-semibold">{projectRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">프로젝트 대기</div>
							<div className="text-2xl font-semibold">{projectRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">프로젝트 완료</div>
							<div className="text-2xl font-semibold">{projectRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>프로젝트 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 담당부서</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 책임자</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 등록일</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 수정일</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 상태코드</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 비고</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 연락처</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 분류</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 우선순위</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 진척률</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 예산코드</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">프로젝트 승인자</dt>
							<dd>{projectRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 04: 근태관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>근태관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedAttendance}
							onValueChange={setSelectedAttendance}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="근태 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{attendanceStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchAttendance}
							onChange={(e) => setSearchAttendance(e.target.value)}
							placeholder="근태 검색..."
						/>
						<Button size="sm">근태 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{attendanceRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{attendancePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>근태 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">근태 전체</div>
							<div className="text-2xl font-semibold">{attendanceRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">근태 진행</div>
							<div className="text-2xl font-semibold">{attendanceRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">근태 대기</div>
							<div className="text-2xl font-semibold">
								{attendanceRows.filter((r) => r.status === 'pending').length}
							</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">근태 완료</div>
							<div className="text-2xl font-semibold">{attendanceRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>근태 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 담당부서</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 책임자</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 등록일</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 수정일</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 상태코드</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 비고</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 연락처</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 분류</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 우선순위</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 진척률</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 예산코드</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">근태 승인자</dt>
							<dd>{attendanceRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 05: 휴가관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>휴가관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedLeave}
							onValueChange={setSelectedLeave}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="휴가 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{leaveStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchLeave}
							onChange={(e) => setSearchLeave(e.target.value)}
							placeholder="휴가 검색..."
						/>
						<Button size="sm">휴가 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{leaveRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{leavePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>휴가 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">휴가 전체</div>
							<div className="text-2xl font-semibold">{leaveRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">휴가 진행</div>
							<div className="text-2xl font-semibold">{leaveRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">휴가 대기</div>
							<div className="text-2xl font-semibold">{leaveRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">휴가 완료</div>
							<div className="text-2xl font-semibold">{leaveRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>휴가 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 담당부서</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 책임자</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 등록일</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 수정일</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 상태코드</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 비고</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 연락처</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 분류</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 우선순위</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 진척률</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 예산코드</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">휴가 승인자</dt>
							<dd>{leaveRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 06: 급여관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>급여관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedPayroll}
							onValueChange={setSelectedPayroll}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="급여 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{payrollStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchPayroll}
							onChange={(e) => setSearchPayroll(e.target.value)}
							placeholder="급여 검색..."
						/>
						<Button size="sm">급여 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{payrollRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{payrollPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>급여 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">급여 전체</div>
							<div className="text-2xl font-semibold">{payrollRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">급여 진행</div>
							<div className="text-2xl font-semibold">{payrollRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">급여 대기</div>
							<div className="text-2xl font-semibold">{payrollRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">급여 완료</div>
							<div className="text-2xl font-semibold">{payrollRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>급여 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 담당부서</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 책임자</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 등록일</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 수정일</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 상태코드</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 비고</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 연락처</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 분류</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 우선순위</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 진척률</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 예산코드</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">급여 승인자</dt>
							<dd>{payrollRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 07: 교육관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>교육관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedTraining}
							onValueChange={setSelectedTraining}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="교육 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{trainingStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchTraining}
							onChange={(e) => setSearchTraining(e.target.value)}
							placeholder="교육 검색..."
						/>
						<Button size="sm">교육 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{trainingRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{trainingPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>교육 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">교육 전체</div>
							<div className="text-2xl font-semibold">{trainingRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">교육 진행</div>
							<div className="text-2xl font-semibold">{trainingRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">교육 대기</div>
							<div className="text-2xl font-semibold">{trainingRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">교육 완료</div>
							<div className="text-2xl font-semibold">{trainingRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>교육 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 담당부서</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 책임자</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 등록일</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 수정일</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 상태코드</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 비고</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 연락처</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 분류</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 우선순위</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 진척률</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 예산코드</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">교육 승인자</dt>
							<dd>{trainingRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 08: 자격증관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>자격증관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedLicense}
							onValueChange={setSelectedLicense}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="자격증 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{licenseStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchLicense}
							onChange={(e) => setSearchLicense(e.target.value)}
							placeholder="자격증 검색..."
						/>
						<Button size="sm">자격증 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{licenseRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{licensePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>자격증 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">자격증 전체</div>
							<div className="text-2xl font-semibold">{licenseRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">자격증 진행</div>
							<div className="text-2xl font-semibold">{licenseRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">자격증 대기</div>
							<div className="text-2xl font-semibold">{licenseRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">자격증 완료</div>
							<div className="text-2xl font-semibold">{licenseRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>자격증 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 담당부서</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 책임자</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 등록일</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 수정일</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 상태코드</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 비고</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 연락처</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 분류</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 우선순위</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 진척률</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 예산코드</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자격증 승인자</dt>
							<dd>{licenseRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 09: 평가관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>평가관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedEvaluation}
							onValueChange={setSelectedEvaluation}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="평가 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{evaluationStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchEvaluation}
							onChange={(e) => setSearchEvaluation(e.target.value)}
							placeholder="평가 검색..."
						/>
						<Button size="sm">평가 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{evaluationRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{evaluationPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>평가 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">평가 전체</div>
							<div className="text-2xl font-semibold">{evaluationRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">평가 진행</div>
							<div className="text-2xl font-semibold">{evaluationRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">평가 대기</div>
							<div className="text-2xl font-semibold">
								{evaluationRows.filter((r) => r.status === 'pending').length}
							</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">평가 완료</div>
							<div className="text-2xl font-semibold">{evaluationRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>평가 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 담당부서</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 책임자</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 등록일</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 수정일</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 상태코드</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 비고</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 연락처</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 분류</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 우선순위</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 진척률</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 예산코드</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">평가 승인자</dt>
							<dd>{evaluationRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 10: 조직관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>조직관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedOrg}
							onValueChange={setSelectedOrg}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="조직 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{orgStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchOrg}
							onChange={(e) => setSearchOrg(e.target.value)}
							placeholder="조직 검색..."
						/>
						<Button size="sm">조직 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{orgRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{orgPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>조직 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">조직 전체</div>
							<div className="text-2xl font-semibold">{orgRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">조직 진행</div>
							<div className="text-2xl font-semibold">{orgRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">조직 대기</div>
							<div className="text-2xl font-semibold">{orgRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">조직 완료</div>
							<div className="text-2xl font-semibold">{orgRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>조직 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 담당부서</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 책임자</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 등록일</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 수정일</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 상태코드</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 비고</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 연락처</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 분류</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 우선순위</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 진척률</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 예산코드</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">조직 승인자</dt>
							<dd>{orgRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 11: 발령관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>발령관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedAssignment}
							onValueChange={setSelectedAssignment}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="발령 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{assignmentStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchAssignment}
							onChange={(e) => setSearchAssignment(e.target.value)}
							placeholder="발령 검색..."
						/>
						<Button size="sm">발령 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{assignmentRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{assignmentPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>발령 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">발령 전체</div>
							<div className="text-2xl font-semibold">{assignmentRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">발령 진행</div>
							<div className="text-2xl font-semibold">{assignmentRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">발령 대기</div>
							<div className="text-2xl font-semibold">
								{assignmentRows.filter((r) => r.status === 'pending').length}
							</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">발령 완료</div>
							<div className="text-2xl font-semibold">{assignmentRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>발령 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 담당부서</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 책임자</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 등록일</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 수정일</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 상태코드</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 비고</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 연락처</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 분류</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 우선순위</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 진척률</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 예산코드</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">발령 승인자</dt>
							<dd>{assignmentRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 12: 출장관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>출장관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedTravel}
							onValueChange={setSelectedTravel}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="출장 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{travelStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchTravel}
							onChange={(e) => setSearchTravel(e.target.value)}
							placeholder="출장 검색..."
						/>
						<Button size="sm">출장 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{travelRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{travelPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>출장 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">출장 전체</div>
							<div className="text-2xl font-semibold">{travelRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">출장 진행</div>
							<div className="text-2xl font-semibold">{travelRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">출장 대기</div>
							<div className="text-2xl font-semibold">{travelRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">출장 완료</div>
							<div className="text-2xl font-semibold">{travelRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>출장 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 담당부서</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 책임자</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 등록일</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 수정일</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 상태코드</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 비고</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 연락처</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 분류</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 우선순위</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 진척률</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 예산코드</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">출장 승인자</dt>
							<dd>{travelRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 13: 경비관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>경비관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedExpense}
							onValueChange={setSelectedExpense}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="경비 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{expenseStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchExpense}
							onChange={(e) => setSearchExpense(e.target.value)}
							placeholder="경비 검색..."
						/>
						<Button size="sm">경비 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{expenseRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{expensePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>경비 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">경비 전체</div>
							<div className="text-2xl font-semibold">{expenseRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">경비 진행</div>
							<div className="text-2xl font-semibold">{expenseRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">경비 대기</div>
							<div className="text-2xl font-semibold">{expenseRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">경비 완료</div>
							<div className="text-2xl font-semibold">{expenseRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>경비 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 담당부서</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 책임자</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 등록일</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 수정일</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 상태코드</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 비고</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 연락처</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 분류</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 우선순위</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 진척률</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 예산코드</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">경비 승인자</dt>
							<dd>{expenseRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 14: 자산관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>자산관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedAsset}
							onValueChange={setSelectedAsset}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="자산 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{assetStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchAsset}
							onChange={(e) => setSearchAsset(e.target.value)}
							placeholder="자산 검색..."
						/>
						<Button size="sm">자산 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{assetRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{assetPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>자산 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">자산 전체</div>
							<div className="text-2xl font-semibold">{assetRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">자산 진행</div>
							<div className="text-2xl font-semibold">{assetRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">자산 대기</div>
							<div className="text-2xl font-semibold">{assetRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">자산 완료</div>
							<div className="text-2xl font-semibold">{assetRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>자산 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 담당부서</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 책임자</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 등록일</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 수정일</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 상태코드</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 비고</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 연락처</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 분류</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 우선순위</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 진척률</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 예산코드</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">자산 승인자</dt>
							<dd>{assetRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 15: 회의실관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>회의실관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedRoom}
							onValueChange={setSelectedRoom}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="회의실 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{roomStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchRoom}
							onChange={(e) => setSearchRoom(e.target.value)}
							placeholder="회의실 검색..."
						/>
						<Button size="sm">회의실 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{roomRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{roomPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>회의실 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">회의실 전체</div>
							<div className="text-2xl font-semibold">{roomRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">회의실 진행</div>
							<div className="text-2xl font-semibold">{roomRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">회의실 대기</div>
							<div className="text-2xl font-semibold">{roomRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">회의실 완료</div>
							<div className="text-2xl font-semibold">{roomRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>회의실 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 담당부서</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 책임자</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 등록일</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 수정일</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 상태코드</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 비고</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 연락처</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 분류</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 우선순위</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 진척률</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 예산코드</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회의실 승인자</dt>
							<dd>{roomRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 16: 공지관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>공지관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedNotice}
							onValueChange={setSelectedNotice}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="공지 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{noticeStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchNotice}
							onChange={(e) => setSearchNotice(e.target.value)}
							placeholder="공지 검색..."
						/>
						<Button size="sm">공지 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{noticeRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{noticePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>공지 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">공지 전체</div>
							<div className="text-2xl font-semibold">{noticeRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">공지 진행</div>
							<div className="text-2xl font-semibold">{noticeRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">공지 대기</div>
							<div className="text-2xl font-semibold">{noticeRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">공지 완료</div>
							<div className="text-2xl font-semibold">{noticeRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>공지 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 담당부서</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 책임자</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 등록일</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 수정일</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 상태코드</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 비고</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 연락처</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 분류</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 우선순위</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 진척률</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 예산코드</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">공지 승인자</dt>
							<dd>{noticeRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 17: 문서관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>문서관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedDocument}
							onValueChange={setSelectedDocument}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="문서 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{documentStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchDocument}
							onChange={(e) => setSearchDocument(e.target.value)}
							placeholder="문서 검색..."
						/>
						<Button size="sm">문서 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{documentRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{documentPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>문서 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">문서 전체</div>
							<div className="text-2xl font-semibold">{documentRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">문서 진행</div>
							<div className="text-2xl font-semibold">{documentRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">문서 대기</div>
							<div className="text-2xl font-semibold">{documentRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">문서 완료</div>
							<div className="text-2xl font-semibold">{documentRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>문서 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 담당부서</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 책임자</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 등록일</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 수정일</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 상태코드</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 비고</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 연락처</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 분류</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 우선순위</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 진척률</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 예산코드</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">문서 승인자</dt>
							<dd>{documentRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 18: 결재관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>결재관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedApproval}
							onValueChange={setSelectedApproval}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="결재 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{approvalStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchApproval}
							onChange={(e) => setSearchApproval(e.target.value)}
							placeholder="결재 검색..."
						/>
						<Button size="sm">결재 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{approvalRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{approvalPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>결재 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">결재 전체</div>
							<div className="text-2xl font-semibold">{approvalRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">결재 진행</div>
							<div className="text-2xl font-semibold">{approvalRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">결재 대기</div>
							<div className="text-2xl font-semibold">{approvalRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">결재 완료</div>
							<div className="text-2xl font-semibold">{approvalRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>결재 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 담당부서</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 책임자</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 등록일</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 수정일</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 상태코드</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 비고</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 연락처</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 분류</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 우선순위</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 진척률</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 예산코드</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">결재 승인자</dt>
							<dd>{approvalRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 19: 거래처관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>거래처관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedClient}
							onValueChange={setSelectedClient}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="거래처 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{clientStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchClient}
							onChange={(e) => setSearchClient(e.target.value)}
							placeholder="거래처 검색..."
						/>
						<Button size="sm">거래처 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{clientRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{clientPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>거래처 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">거래처 전체</div>
							<div className="text-2xl font-semibold">{clientRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">거래처 진행</div>
							<div className="text-2xl font-semibold">{clientRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">거래처 대기</div>
							<div className="text-2xl font-semibold">{clientRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">거래처 완료</div>
							<div className="text-2xl font-semibold">{clientRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>거래처 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 담당부서</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 책임자</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 등록일</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 수정일</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 상태코드</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 비고</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 연락처</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 분류</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 우선순위</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 진척률</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 예산코드</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">거래처 승인자</dt>
							<dd>{clientRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 20: 계약관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>계약관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedContract}
							onValueChange={setSelectedContract}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="계약 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{contractStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchContract}
							onChange={(e) => setSearchContract(e.target.value)}
							placeholder="계약 검색..."
						/>
						<Button size="sm">계약 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{contractRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{contractPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>계약 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">계약 전체</div>
							<div className="text-2xl font-semibold">{contractRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">계약 진행</div>
							<div className="text-2xl font-semibold">{contractRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">계약 대기</div>
							<div className="text-2xl font-semibold">{contractRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">계약 완료</div>
							<div className="text-2xl font-semibold">{contractRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>계약 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 담당부서</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 책임자</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 등록일</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 수정일</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 상태코드</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 비고</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 연락처</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 분류</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 우선순위</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 진척률</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 예산코드</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">계약 승인자</dt>
							<dd>{contractRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 21: 매출관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>매출관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedSales}
							onValueChange={setSelectedSales}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="매출 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{salesStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchSales}
							onChange={(e) => setSearchSales(e.target.value)}
							placeholder="매출 검색..."
						/>
						<Button size="sm">매출 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{salesRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{salesPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>매출 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">매출 전체</div>
							<div className="text-2xl font-semibold">{salesRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">매출 진행</div>
							<div className="text-2xl font-semibold">{salesRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">매출 대기</div>
							<div className="text-2xl font-semibold">{salesRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">매출 완료</div>
							<div className="text-2xl font-semibold">{salesRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>매출 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 담당부서</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 책임자</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 등록일</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 수정일</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 상태코드</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 비고</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 연락처</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 분류</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 우선순위</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 진척률</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 예산코드</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">매출 승인자</dt>
							<dd>{salesRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 22: 구매관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>구매관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedPurchase}
							onValueChange={setSelectedPurchase}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="구매 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{purchaseStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchPurchase}
							onChange={(e) => setSearchPurchase(e.target.value)}
							placeholder="구매 검색..."
						/>
						<Button size="sm">구매 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{purchaseRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{purchasePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>구매 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">구매 전체</div>
							<div className="text-2xl font-semibold">{purchaseRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">구매 진행</div>
							<div className="text-2xl font-semibold">{purchaseRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">구매 대기</div>
							<div className="text-2xl font-semibold">{purchaseRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">구매 완료</div>
							<div className="text-2xl font-semibold">{purchaseRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>구매 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 담당부서</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 책임자</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 등록일</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 수정일</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 상태코드</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 비고</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 연락처</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 분류</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 우선순위</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 진척률</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 예산코드</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">구매 승인자</dt>
							<dd>{purchaseRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 23: 재고관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>재고관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedInventory}
							onValueChange={setSelectedInventory}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="재고 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{inventoryStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchInventory}
							onChange={(e) => setSearchInventory(e.target.value)}
							placeholder="재고 검색..."
						/>
						<Button size="sm">재고 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{inventoryRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{inventoryPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>재고 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">재고 전체</div>
							<div className="text-2xl font-semibold">{inventoryRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">재고 진행</div>
							<div className="text-2xl font-semibold">{inventoryRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">재고 대기</div>
							<div className="text-2xl font-semibold">{inventoryRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">재고 완료</div>
							<div className="text-2xl font-semibold">{inventoryRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>재고 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 담당부서</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 책임자</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 등록일</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 수정일</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 상태코드</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 비고</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 연락처</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 분류</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 우선순위</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 진척률</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 예산코드</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">재고 승인자</dt>
							<dd>{inventoryRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 24: 품질관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>품질관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedQuality}
							onValueChange={setSelectedQuality}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="품질 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{qualityStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchQuality}
							onChange={(e) => setSearchQuality(e.target.value)}
							placeholder="품질 검색..."
						/>
						<Button size="sm">품질 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{qualityRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{qualityPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>품질 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">품질 전체</div>
							<div className="text-2xl font-semibold">{qualityRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">품질 진행</div>
							<div className="text-2xl font-semibold">{qualityRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">품질 대기</div>
							<div className="text-2xl font-semibold">{qualityRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">품질 완료</div>
							<div className="text-2xl font-semibold">{qualityRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>품질 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 담당부서</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 책임자</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 등록일</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 수정일</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 상태코드</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 비고</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 연락처</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 분류</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 우선순위</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 진척률</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 예산코드</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">품질 승인자</dt>
							<dd>{qualityRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 25: 생산관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>생산관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedProduction}
							onValueChange={setSelectedProduction}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="생산 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{productionStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchProduction}
							onChange={(e) => setSearchProduction(e.target.value)}
							placeholder="생산 검색..."
						/>
						<Button size="sm">생산 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{productionRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{productionPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>생산 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">생산 전체</div>
							<div className="text-2xl font-semibold">{productionRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">생산 진행</div>
							<div className="text-2xl font-semibold">{productionRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">생산 대기</div>
							<div className="text-2xl font-semibold">
								{productionRows.filter((r) => r.status === 'pending').length}
							</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">생산 완료</div>
							<div className="text-2xl font-semibold">{productionRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>생산 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 담당부서</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 책임자</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 등록일</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 수정일</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 상태코드</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 비고</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 연락처</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 분류</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 우선순위</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 진척률</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 예산코드</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">생산 승인자</dt>
							<dd>{productionRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 26: 설비관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>설비관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedEquipment}
							onValueChange={setSelectedEquipment}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="설비 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{equipmentStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchEquipment}
							onChange={(e) => setSearchEquipment(e.target.value)}
							placeholder="설비 검색..."
						/>
						<Button size="sm">설비 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{equipmentRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{equipmentPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>설비 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">설비 전체</div>
							<div className="text-2xl font-semibold">{equipmentRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">설비 진행</div>
							<div className="text-2xl font-semibold">{equipmentRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">설비 대기</div>
							<div className="text-2xl font-semibold">{equipmentRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">설비 완료</div>
							<div className="text-2xl font-semibold">{equipmentRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>설비 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 담당부서</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 책임자</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 등록일</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 수정일</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 상태코드</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 비고</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 연락처</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 분류</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 우선순위</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 진척률</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 예산코드</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설비 승인자</dt>
							<dd>{equipmentRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 27: 안전관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>안전관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedSafety}
							onValueChange={setSelectedSafety}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="안전 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{safetyStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchSafety}
							onChange={(e) => setSearchSafety(e.target.value)}
							placeholder="안전 검색..."
						/>
						<Button size="sm">안전 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{safetyRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{safetyPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>안전 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">안전 전체</div>
							<div className="text-2xl font-semibold">{safetyRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">안전 진행</div>
							<div className="text-2xl font-semibold">{safetyRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">안전 대기</div>
							<div className="text-2xl font-semibold">{safetyRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">안전 완료</div>
							<div className="text-2xl font-semibold">{safetyRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>안전 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 담당부서</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 책임자</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 등록일</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 수정일</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 상태코드</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 비고</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 연락처</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 분류</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 우선순위</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 진척률</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 예산코드</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">안전 승인자</dt>
							<dd>{safetyRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 28: 민원관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>민원관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedComplaint}
							onValueChange={setSelectedComplaint}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="민원 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{complaintStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchComplaint}
							onChange={(e) => setSearchComplaint(e.target.value)}
							placeholder="민원 검색..."
						/>
						<Button size="sm">민원 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{complaintRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{complaintPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>민원 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">민원 전체</div>
							<div className="text-2xl font-semibold">{complaintRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">민원 진행</div>
							<div className="text-2xl font-semibold">{complaintRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">민원 대기</div>
							<div className="text-2xl font-semibold">{complaintRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">민원 완료</div>
							<div className="text-2xl font-semibold">{complaintRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>민원 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 담당부서</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 책임자</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 등록일</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 수정일</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 상태코드</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 비고</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 연락처</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 분류</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 우선순위</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 진척률</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 예산코드</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">민원 승인자</dt>
							<dd>{complaintRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 29: 고객관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>고객관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedCustomer}
							onValueChange={setSelectedCustomer}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="고객 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{customerStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchCustomer}
							onChange={(e) => setSearchCustomer(e.target.value)}
							placeholder="고객 검색..."
						/>
						<Button size="sm">고객 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{customerRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{customerPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>고객 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">고객 전체</div>
							<div className="text-2xl font-semibold">{customerRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">고객 진행</div>
							<div className="text-2xl font-semibold">{customerRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">고객 대기</div>
							<div className="text-2xl font-semibold">{customerRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">고객 완료</div>
							<div className="text-2xl font-semibold">{customerRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>고객 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 담당부서</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 책임자</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 등록일</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 수정일</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 상태코드</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 비고</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 연락처</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 분류</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 우선순위</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 진척률</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 예산코드</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">고객 승인자</dt>
							<dd>{customerRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 30: 마케팅관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>마케팅관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedMarketing}
							onValueChange={setSelectedMarketing}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="마케팅 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{marketingStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchMarketing}
							onChange={(e) => setSearchMarketing(e.target.value)}
							placeholder="마케팅 검색..."
						/>
						<Button size="sm">마케팅 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{marketingRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{marketingPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>마케팅 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">마케팅 전체</div>
							<div className="text-2xl font-semibold">{marketingRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">마케팅 진행</div>
							<div className="text-2xl font-semibold">{marketingRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">마케팅 대기</div>
							<div className="text-2xl font-semibold">{marketingRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">마케팅 완료</div>
							<div className="text-2xl font-semibold">{marketingRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>마케팅 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 담당부서</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 책임자</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 등록일</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 수정일</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 상태코드</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 비고</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 연락처</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 분류</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 우선순위</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 진척률</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 예산코드</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">마케팅 승인자</dt>
							<dd>{marketingRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 31: 영업관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>영업관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedBiz}
							onValueChange={setSelectedBiz}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="영업 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{bizStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchBiz}
							onChange={(e) => setSearchBiz(e.target.value)}
							placeholder="영업 검색..."
						/>
						<Button size="sm">영업 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{bizRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{bizPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>영업 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">영업 전체</div>
							<div className="text-2xl font-semibold">{bizRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">영업 진행</div>
							<div className="text-2xl font-semibold">{bizRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">영업 대기</div>
							<div className="text-2xl font-semibold">{bizRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">영업 완료</div>
							<div className="text-2xl font-semibold">{bizRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>영업 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 담당부서</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 책임자</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 등록일</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 수정일</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 상태코드</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 비고</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 연락처</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 분류</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 우선순위</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 진척률</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 예산코드</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">영업 승인자</dt>
							<dd>{bizRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 32: 회계관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>회계관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedAccounting}
							onValueChange={setSelectedAccounting}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="회계 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{accountingStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchAccounting}
							onChange={(e) => setSearchAccounting(e.target.value)}
							placeholder="회계 검색..."
						/>
						<Button size="sm">회계 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{accountingRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{accountingPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>회계 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">회계 전체</div>
							<div className="text-2xl font-semibold">{accountingRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">회계 진행</div>
							<div className="text-2xl font-semibold">{accountingRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">회계 대기</div>
							<div className="text-2xl font-semibold">
								{accountingRows.filter((r) => r.status === 'pending').length}
							</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">회계 완료</div>
							<div className="text-2xl font-semibold">{accountingRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>회계 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 담당부서</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 책임자</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 등록일</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 수정일</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 상태코드</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 비고</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 연락처</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 분류</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 우선순위</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 진척률</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 예산코드</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">회계 승인자</dt>
							<dd>{accountingRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 33: 예산관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>예산관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedBudget}
							onValueChange={setSelectedBudget}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="예산 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{budgetStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchBudget}
							onChange={(e) => setSearchBudget(e.target.value)}
							placeholder="예산 검색..."
						/>
						<Button size="sm">예산 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{budgetRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{budgetPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>예산 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">예산 전체</div>
							<div className="text-2xl font-semibold">{budgetRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">예산 진행</div>
							<div className="text-2xl font-semibold">{budgetRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">예산 대기</div>
							<div className="text-2xl font-semibold">{budgetRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">예산 완료</div>
							<div className="text-2xl font-semibold">{budgetRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>예산 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 담당부서</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 책임자</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 등록일</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 수정일</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 상태코드</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 비고</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 연락처</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 분류</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 우선순위</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 진척률</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 예산코드</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">예산 승인자</dt>
							<dd>{budgetRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 34: 세무관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>세무관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedTax}
							onValueChange={setSelectedTax}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="세무 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{taxStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchTax}
							onChange={(e) => setSearchTax(e.target.value)}
							placeholder="세무 검색..."
						/>
						<Button size="sm">세무 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{taxRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{taxPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>세무 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">세무 전체</div>
							<div className="text-2xl font-semibold">{taxRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">세무 진행</div>
							<div className="text-2xl font-semibold">{taxRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">세무 대기</div>
							<div className="text-2xl font-semibold">{taxRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">세무 완료</div>
							<div className="text-2xl font-semibold">{taxRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>세무 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 담당부서</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 책임자</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 등록일</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 수정일</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 상태코드</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 비고</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 연락처</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 분류</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 우선순위</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 진척률</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 예산코드</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">세무 승인자</dt>
							<dd>{taxRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 35: 감사관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>감사관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedAudit}
							onValueChange={setSelectedAudit}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="감사 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{auditStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchAudit}
							onChange={(e) => setSearchAudit(e.target.value)}
							placeholder="감사 검색..."
						/>
						<Button size="sm">감사 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{auditRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{auditPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>감사 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">감사 전체</div>
							<div className="text-2xl font-semibold">{auditRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">감사 진행</div>
							<div className="text-2xl font-semibold">{auditRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">감사 대기</div>
							<div className="text-2xl font-semibold">{auditRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">감사 완료</div>
							<div className="text-2xl font-semibold">{auditRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>감사 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 담당부서</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 책임자</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 등록일</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 수정일</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 상태코드</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 비고</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 연락처</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 분류</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 우선순위</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 진척률</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 예산코드</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">감사 승인자</dt>
							<dd>{auditRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 36: 법무관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>법무관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedLegal}
							onValueChange={setSelectedLegal}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="법무 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{legalStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchLegal}
							onChange={(e) => setSearchLegal(e.target.value)}
							placeholder="법무 검색..."
						/>
						<Button size="sm">법무 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{legalRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{legalPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>법무 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">법무 전체</div>
							<div className="text-2xl font-semibold">{legalRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">법무 진행</div>
							<div className="text-2xl font-semibold">{legalRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">법무 대기</div>
							<div className="text-2xl font-semibold">{legalRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">법무 완료</div>
							<div className="text-2xl font-semibold">{legalRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>법무 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 담당부서</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 책임자</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 등록일</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 수정일</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 상태코드</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 비고</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 연락처</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 분류</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 우선순위</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 진척률</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 예산코드</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">법무 승인자</dt>
							<dd>{legalRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 37: 보안관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>보안관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedSecurity}
							onValueChange={setSelectedSecurity}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="보안 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{securityStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchSecurity}
							onChange={(e) => setSearchSecurity(e.target.value)}
							placeholder="보안 검색..."
						/>
						<Button size="sm">보안 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{securityRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{securityPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>보안 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">보안 전체</div>
							<div className="text-2xl font-semibold">{securityRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">보안 진행</div>
							<div className="text-2xl font-semibold">{securityRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">보안 대기</div>
							<div className="text-2xl font-semibold">{securityRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">보안 완료</div>
							<div className="text-2xl font-semibold">{securityRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>보안 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 담당부서</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 책임자</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 등록일</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 수정일</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 상태코드</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 비고</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 연락처</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 분류</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 우선순위</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 진척률</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 예산코드</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">보안 승인자</dt>
							<dd>{securityRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 38: 전산관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>전산관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedIt}
							onValueChange={setSelectedIt}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="전산 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{itStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchIt}
							onChange={(e) => setSearchIt(e.target.value)}
							placeholder="전산 검색..."
						/>
						<Button size="sm">전산 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{itRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{itPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>전산 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">전산 전체</div>
							<div className="text-2xl font-semibold">{itRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">전산 진행</div>
							<div className="text-2xl font-semibold">{itRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">전산 대기</div>
							<div className="text-2xl font-semibold">{itRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">전산 완료</div>
							<div className="text-2xl font-semibold">{itRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>전산 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 담당부서</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 책임자</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 등록일</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 수정일</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 상태코드</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 비고</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 연락처</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 분류</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 우선순위</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 진척률</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 예산코드</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">전산 승인자</dt>
							<dd>{itRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 39: 네트워크관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>네트워크관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedNetwork}
							onValueChange={setSelectedNetwork}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="네트워크 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{networkStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchNetwork}
							onChange={(e) => setSearchNetwork(e.target.value)}
							placeholder="네트워크 검색..."
						/>
						<Button size="sm">네트워크 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{networkRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{networkPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>네트워크 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">네트워크 전체</div>
							<div className="text-2xl font-semibold">{networkRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">네트워크 진행</div>
							<div className="text-2xl font-semibold">{networkRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">네트워크 대기</div>
							<div className="text-2xl font-semibold">{networkRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">네트워크 완료</div>
							<div className="text-2xl font-semibold">{networkRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>네트워크 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 담당부서</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 책임자</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 등록일</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 수정일</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 상태코드</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 비고</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 연락처</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 분류</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 우선순위</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 진척률</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 예산코드</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">네트워크 승인자</dt>
							<dd>{networkRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 40: 서버관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>서버관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedServer}
							onValueChange={setSelectedServer}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="서버 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{serverStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchServer}
							onChange={(e) => setSearchServer(e.target.value)}
							placeholder="서버 검색..."
						/>
						<Button size="sm">서버 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{serverRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{serverPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>서버 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">서버 전체</div>
							<div className="text-2xl font-semibold">{serverRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">서버 진행</div>
							<div className="text-2xl font-semibold">{serverRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">서버 대기</div>
							<div className="text-2xl font-semibold">{serverRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">서버 완료</div>
							<div className="text-2xl font-semibold">{serverRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>서버 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 담당부서</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 책임자</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 등록일</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 수정일</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 상태코드</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 비고</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 연락처</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 분류</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 우선순위</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 진척률</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 예산코드</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">서버 승인자</dt>
							<dd>{serverRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 41: 데이터관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>데이터관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedDataset}
							onValueChange={setSelectedDataset}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="데이터 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{datasetStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchDataset}
							onChange={(e) => setSearchDataset(e.target.value)}
							placeholder="데이터 검색..."
						/>
						<Button size="sm">데이터 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{datasetRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{datasetPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>데이터 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">데이터 전체</div>
							<div className="text-2xl font-semibold">{datasetRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">데이터 진행</div>
							<div className="text-2xl font-semibold">{datasetRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">데이터 대기</div>
							<div className="text-2xl font-semibold">{datasetRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">데이터 완료</div>
							<div className="text-2xl font-semibold">{datasetRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>데이터 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 담당부서</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 책임자</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 등록일</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 수정일</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 상태코드</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 비고</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 연락처</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 분류</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 우선순위</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 진척률</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 예산코드</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">데이터 승인자</dt>
							<dd>{datasetRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 42: 백업관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>백업관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedBackup}
							onValueChange={setSelectedBackup}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="백업 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{backupStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchBackup}
							onChange={(e) => setSearchBackup(e.target.value)}
							placeholder="백업 검색..."
						/>
						<Button size="sm">백업 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{backupRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{backupPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>백업 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">백업 전체</div>
							<div className="text-2xl font-semibold">{backupRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">백업 진행</div>
							<div className="text-2xl font-semibold">{backupRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">백업 대기</div>
							<div className="text-2xl font-semibold">{backupRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">백업 완료</div>
							<div className="text-2xl font-semibold">{backupRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>백업 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 담당부서</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 책임자</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 등록일</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 수정일</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 상태코드</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 비고</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 연락처</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 분류</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 우선순위</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 진척률</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 예산코드</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">백업 승인자</dt>
							<dd>{backupRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 43: 라이선스관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>라이선스관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedLicenseKey}
							onValueChange={setSelectedLicenseKey}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="라이선스 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{licenseKeyStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchLicenseKey}
							onChange={(e) => setSearchLicenseKey(e.target.value)}
							placeholder="라이선스 검색..."
						/>
						<Button size="sm">라이선스 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{licenseKeyRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{licenseKeyPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>라이선스 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">라이선스 전체</div>
							<div className="text-2xl font-semibold">{licenseKeyRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">라이선스 진행</div>
							<div className="text-2xl font-semibold">{licenseKeyRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">라이선스 대기</div>
							<div className="text-2xl font-semibold">
								{licenseKeyRows.filter((r) => r.status === 'pending').length}
							</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">라이선스 완료</div>
							<div className="text-2xl font-semibold">{licenseKeyRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>라이선스 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 담당부서</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 책임자</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 등록일</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 수정일</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 상태코드</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 비고</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 연락처</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 분류</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 우선순위</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 진척률</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 예산코드</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">라이선스 승인자</dt>
							<dd>{licenseKeyRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 44: 권한관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>권한관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedPermission}
							onValueChange={setSelectedPermission}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="권한 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{permissionStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchPermission}
							onChange={(e) => setSearchPermission(e.target.value)}
							placeholder="권한 검색..."
						/>
						<Button size="sm">권한 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{permissionRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{permissionPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>권한 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">권한 전체</div>
							<div className="text-2xl font-semibold">{permissionRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">권한 진행</div>
							<div className="text-2xl font-semibold">{permissionRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">권한 대기</div>
							<div className="text-2xl font-semibold">
								{permissionRows.filter((r) => r.status === 'pending').length}
							</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">권한 완료</div>
							<div className="text-2xl font-semibold">{permissionRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>권한 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 담당부서</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 책임자</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 등록일</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 수정일</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 상태코드</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 비고</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 연락처</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 분류</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 우선순위</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 진척률</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 예산코드</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">권한 승인자</dt>
							<dd>{permissionRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 45: 메뉴관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>메뉴관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedMenu}
							onValueChange={setSelectedMenu}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="메뉴 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{menuStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchMenu}
							onChange={(e) => setSearchMenu(e.target.value)}
							placeholder="메뉴 검색..."
						/>
						<Button size="sm">메뉴 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{menuRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{menuPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>메뉴 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">메뉴 전체</div>
							<div className="text-2xl font-semibold">{menuRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">메뉴 진행</div>
							<div className="text-2xl font-semibold">{menuRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">메뉴 대기</div>
							<div className="text-2xl font-semibold">{menuRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">메뉴 완료</div>
							<div className="text-2xl font-semibold">{menuRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>메뉴 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 담당부서</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 책임자</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 등록일</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 수정일</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 상태코드</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 비고</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 연락처</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 분류</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 우선순위</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 진척률</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 예산코드</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메뉴 승인자</dt>
							<dd>{menuRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 46: 코드관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>코드관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedCommonCode}
							onValueChange={setSelectedCommonCode}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="코드 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{commonCodeStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchCommonCode}
							onChange={(e) => setSearchCommonCode(e.target.value)}
							placeholder="코드 검색..."
						/>
						<Button size="sm">코드 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{commonCodeRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{commonCodePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>코드 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">코드 전체</div>
							<div className="text-2xl font-semibold">{commonCodeRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">코드 진행</div>
							<div className="text-2xl font-semibold">{commonCodeRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">코드 대기</div>
							<div className="text-2xl font-semibold">
								{commonCodeRows.filter((r) => r.status === 'pending').length}
							</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">코드 완료</div>
							<div className="text-2xl font-semibold">{commonCodeRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>코드 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 담당부서</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 책임자</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 등록일</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 수정일</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 상태코드</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 비고</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 연락처</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 분류</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 우선순위</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 진척률</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 예산코드</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">코드 승인자</dt>
							<dd>{commonCodeRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 47: 배치관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>배치관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedBatch}
							onValueChange={setSelectedBatch}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="배치 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{batchStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchBatch}
							onChange={(e) => setSearchBatch(e.target.value)}
							placeholder="배치 검색..."
						/>
						<Button size="sm">배치 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{batchRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{batchPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>배치 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">배치 전체</div>
							<div className="text-2xl font-semibold">{batchRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">배치 진행</div>
							<div className="text-2xl font-semibold">{batchRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">배치 대기</div>
							<div className="text-2xl font-semibold">{batchRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">배치 완료</div>
							<div className="text-2xl font-semibold">{batchRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>배치 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 담당부서</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 책임자</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 등록일</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 수정일</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 상태코드</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 비고</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 연락처</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 분류</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 우선순위</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 진척률</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 예산코드</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">배치 승인자</dt>
							<dd>{batchRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 48: 로그관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>로그관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedLog}
							onValueChange={setSelectedLog}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="로그 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{logStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchLog}
							onChange={(e) => setSearchLog(e.target.value)}
							placeholder="로그 검색..."
						/>
						<Button size="sm">로그 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{logRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{logPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>로그 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">로그 전체</div>
							<div className="text-2xl font-semibold">{logRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">로그 진행</div>
							<div className="text-2xl font-semibold">{logRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">로그 대기</div>
							<div className="text-2xl font-semibold">{logRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">로그 완료</div>
							<div className="text-2xl font-semibold">{logRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>로그 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 담당부서</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 책임자</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 등록일</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 수정일</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 상태코드</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 비고</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 연락처</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 분류</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 우선순위</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 진척률</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 예산코드</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">로그 승인자</dt>
							<dd>{logRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 49: 알림관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>알림관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedAlert}
							onValueChange={setSelectedAlert}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="알림 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{alertStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchAlert}
							onChange={(e) => setSearchAlert(e.target.value)}
							placeholder="알림 검색..."
						/>
						<Button size="sm">알림 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{alertRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{alertPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>알림 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">알림 전체</div>
							<div className="text-2xl font-semibold">{alertRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">알림 진행</div>
							<div className="text-2xl font-semibold">{alertRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">알림 대기</div>
							<div className="text-2xl font-semibold">{alertRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">알림 완료</div>
							<div className="text-2xl font-semibold">{alertRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>알림 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 담당부서</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 책임자</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 등록일</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 수정일</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 상태코드</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 비고</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 연락처</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 분류</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 우선순위</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 진척률</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 예산코드</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">알림 승인자</dt>
							<dd>{alertRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 50: 메일관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>메일관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedMail}
							onValueChange={setSelectedMail}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="메일 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{mailStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchMail}
							onChange={(e) => setSearchMail(e.target.value)}
							placeholder="메일 검색..."
						/>
						<Button size="sm">메일 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{mailRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{mailPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>메일 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">메일 전체</div>
							<div className="text-2xl font-semibold">{mailRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">메일 진행</div>
							<div className="text-2xl font-semibold">{mailRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">메일 대기</div>
							<div className="text-2xl font-semibold">{mailRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">메일 완료</div>
							<div className="text-2xl font-semibold">{mailRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>메일 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 담당부서</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 책임자</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 등록일</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 수정일</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 상태코드</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 비고</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 연락처</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 분류</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 우선순위</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 진척률</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 예산코드</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">메일 승인자</dt>
							<dd>{mailRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 51: 게시판관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>게시판관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedBoard}
							onValueChange={setSelectedBoard}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="게시판 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{boardStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchBoard}
							onChange={(e) => setSearchBoard(e.target.value)}
							placeholder="게시판 검색..."
						/>
						<Button size="sm">게시판 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{boardRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{boardPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>게시판 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">게시판 전체</div>
							<div className="text-2xl font-semibold">{boardRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">게시판 진행</div>
							<div className="text-2xl font-semibold">{boardRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">게시판 대기</div>
							<div className="text-2xl font-semibold">{boardRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">게시판 완료</div>
							<div className="text-2xl font-semibold">{boardRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>게시판 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 담당부서</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 책임자</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 등록일</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 수정일</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 상태코드</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 비고</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 연락처</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 분류</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 우선순위</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 진척률</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 예산코드</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">게시판 승인자</dt>
							<dd>{boardRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 52: 댓글관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>댓글관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedComment}
							onValueChange={setSelectedComment}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="댓글 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{commentStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchComment}
							onChange={(e) => setSearchComment(e.target.value)}
							placeholder="댓글 검색..."
						/>
						<Button size="sm">댓글 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{commentRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{commentPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>댓글 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">댓글 전체</div>
							<div className="text-2xl font-semibold">{commentRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">댓글 진행</div>
							<div className="text-2xl font-semibold">{commentRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">댓글 대기</div>
							<div className="text-2xl font-semibold">{commentRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">댓글 완료</div>
							<div className="text-2xl font-semibold">{commentRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>댓글 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 담당부서</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 책임자</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 등록일</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 수정일</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 상태코드</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 비고</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 연락처</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 분류</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 우선순위</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 진척률</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 예산코드</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">댓글 승인자</dt>
							<dd>{commentRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 53: 첨부관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>첨부관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedAttach}
							onValueChange={setSelectedAttach}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="첨부 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{attachStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchAttach}
							onChange={(e) => setSearchAttach(e.target.value)}
							placeholder="첨부 검색..."
						/>
						<Button size="sm">첨부 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{attachRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{attachPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>첨부 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">첨부 전체</div>
							<div className="text-2xl font-semibold">{attachRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">첨부 진행</div>
							<div className="text-2xl font-semibold">{attachRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">첨부 대기</div>
							<div className="text-2xl font-semibold">{attachRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">첨부 완료</div>
							<div className="text-2xl font-semibold">{attachRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>첨부 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 담당부서</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 책임자</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 등록일</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 수정일</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 상태코드</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 비고</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 연락처</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 분류</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 우선순위</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 진척률</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 예산코드</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">첨부 승인자</dt>
							<dd>{attachRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 54: 이미지관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>이미지관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedImage}
							onValueChange={setSelectedImage}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="이미지 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{imageStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchImage}
							onChange={(e) => setSearchImage(e.target.value)}
							placeholder="이미지 검색..."
						/>
						<Button size="sm">이미지 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{imageRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{imagePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>이미지 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">이미지 전체</div>
							<div className="text-2xl font-semibold">{imageRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">이미지 진행</div>
							<div className="text-2xl font-semibold">{imageRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">이미지 대기</div>
							<div className="text-2xl font-semibold">{imageRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">이미지 완료</div>
							<div className="text-2xl font-semibold">{imageRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>이미지 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 담당부서</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 책임자</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 등록일</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 수정일</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 상태코드</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 비고</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 연락처</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 분류</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 우선순위</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 진척률</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 예산코드</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">이미지 승인자</dt>
							<dd>{imageRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 55: 동영상관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>동영상관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedVideo}
							onValueChange={setSelectedVideo}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="동영상 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{videoStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchVideo}
							onChange={(e) => setSearchVideo(e.target.value)}
							placeholder="동영상 검색..."
						/>
						<Button size="sm">동영상 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{videoRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{videoPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>동영상 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">동영상 전체</div>
							<div className="text-2xl font-semibold">{videoRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">동영상 진행</div>
							<div className="text-2xl font-semibold">{videoRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">동영상 대기</div>
							<div className="text-2xl font-semibold">{videoRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">동영상 완료</div>
							<div className="text-2xl font-semibold">{videoRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>동영상 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 담당부서</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 책임자</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 등록일</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 수정일</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 상태코드</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 비고</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 연락처</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 분류</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 우선순위</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 진척률</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 예산코드</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">동영상 승인자</dt>
							<dd>{videoRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 56: 통계관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>통계관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedStat}
							onValueChange={setSelectedStat}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="통계 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{statStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchStat}
							onChange={(e) => setSearchStat(e.target.value)}
							placeholder="통계 검색..."
						/>
						<Button size="sm">통계 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{statRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{statPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>통계 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">통계 전체</div>
							<div className="text-2xl font-semibold">{statRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">통계 진행</div>
							<div className="text-2xl font-semibold">{statRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">통계 대기</div>
							<div className="text-2xl font-semibold">{statRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">통계 완료</div>
							<div className="text-2xl font-semibold">{statRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>통계 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 담당부서</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 책임자</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 등록일</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 수정일</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 상태코드</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 비고</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 연락처</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 분류</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 우선순위</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 진척률</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 예산코드</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">통계 승인자</dt>
							<dd>{statRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 57: 리포트관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>리포트관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedReport}
							onValueChange={setSelectedReport}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="리포트 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{reportStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchReport}
							onChange={(e) => setSearchReport(e.target.value)}
							placeholder="리포트 검색..."
						/>
						<Button size="sm">리포트 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{reportRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{reportPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>리포트 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">리포트 전체</div>
							<div className="text-2xl font-semibold">{reportRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">리포트 진행</div>
							<div className="text-2xl font-semibold">{reportRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">리포트 대기</div>
							<div className="text-2xl font-semibold">{reportRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">리포트 완료</div>
							<div className="text-2xl font-semibold">{reportRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>리포트 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 담당부서</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 책임자</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 등록일</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 수정일</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 상태코드</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 비고</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 연락처</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 분류</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 우선순위</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 진척률</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 예산코드</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">리포트 승인자</dt>
							<dd>{reportRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 58: 대시보드관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>대시보드관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedDashboard}
							onValueChange={setSelectedDashboard}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="대시보드 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{dashboardStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchDashboard}
							onChange={(e) => setSearchDashboard(e.target.value)}
							placeholder="대시보드 검색..."
						/>
						<Button size="sm">대시보드 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{dashboardRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{dashboardPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>대시보드 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">대시보드 전체</div>
							<div className="text-2xl font-semibold">{dashboardRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">대시보드 진행</div>
							<div className="text-2xl font-semibold">{dashboardRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">대시보드 대기</div>
							<div className="text-2xl font-semibold">{dashboardRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">대시보드 완료</div>
							<div className="text-2xl font-semibold">{dashboardRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>대시보드 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 담당부서</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 책임자</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 등록일</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 수정일</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 상태코드</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 비고</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 연락처</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 분류</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 우선순위</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 진척률</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 예산코드</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">대시보드 승인자</dt>
							<dd>{dashboardRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 59: 캘린더관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>캘린더관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedCalendar}
							onValueChange={setSelectedCalendar}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="캘린더 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{calendarStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchCalendar}
							onChange={(e) => setSearchCalendar(e.target.value)}
							placeholder="캘린더 검색..."
						/>
						<Button size="sm">캘린더 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{calendarRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{calendarPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>캘린더 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">캘린더 전체</div>
							<div className="text-2xl font-semibold">{calendarRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">캘린더 진행</div>
							<div className="text-2xl font-semibold">{calendarRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">캘린더 대기</div>
							<div className="text-2xl font-semibold">{calendarRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">캘린더 완료</div>
							<div className="text-2xl font-semibold">{calendarRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>캘린더 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 담당부서</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 책임자</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 등록일</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 수정일</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 상태코드</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 비고</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 연락처</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 분류</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 우선순위</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 진척률</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 예산코드</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">캘린더 승인자</dt>
							<dd>{calendarRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 60: 일정관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>일정관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedSchedule}
							onValueChange={setSelectedSchedule}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="일정 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{scheduleStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchSchedule}
							onChange={(e) => setSearchSchedule(e.target.value)}
							placeholder="일정 검색..."
						/>
						<Button size="sm">일정 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{scheduleRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{schedulePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>일정 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">일정 전체</div>
							<div className="text-2xl font-semibold">{scheduleRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">일정 진행</div>
							<div className="text-2xl font-semibold">{scheduleRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">일정 대기</div>
							<div className="text-2xl font-semibold">{scheduleRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">일정 완료</div>
							<div className="text-2xl font-semibold">{scheduleRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>일정 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 담당부서</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 책임자</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 등록일</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 수정일</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 상태코드</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 비고</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 연락처</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 분류</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 우선순위</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 진척률</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 예산코드</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">일정 승인자</dt>
							<dd>{scheduleRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 61: 연락처관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>연락처관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedPhone}
							onValueChange={setSelectedPhone}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="연락처 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{phoneStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchPhone}
							onChange={(e) => setSearchPhone(e.target.value)}
							placeholder="연락처 검색..."
						/>
						<Button size="sm">연락처 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{phoneRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{phonePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>연락처 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">연락처 전체</div>
							<div className="text-2xl font-semibold">{phoneRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">연락처 진행</div>
							<div className="text-2xl font-semibold">{phoneRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">연락처 대기</div>
							<div className="text-2xl font-semibold">{phoneRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">연락처 완료</div>
							<div className="text-2xl font-semibold">{phoneRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>연락처 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 담당부서</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 책임자</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 등록일</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 수정일</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 상태코드</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 비고</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 연락처</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 분류</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 우선순위</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 진척률</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 예산코드</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">연락처 승인자</dt>
							<dd>{phoneRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 62: 주소록관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>주소록관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedAddrbook}
							onValueChange={setSelectedAddrbook}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="주소록 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{addrbookStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchAddrbook}
							onChange={(e) => setSearchAddrbook(e.target.value)}
							placeholder="주소록 검색..."
						/>
						<Button size="sm">주소록 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{addrbookRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{addrbookPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>주소록 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">주소록 전체</div>
							<div className="text-2xl font-semibold">{addrbookRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">주소록 진행</div>
							<div className="text-2xl font-semibold">{addrbookRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">주소록 대기</div>
							<div className="text-2xl font-semibold">{addrbookRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">주소록 완료</div>
							<div className="text-2xl font-semibold">{addrbookRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>주소록 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 담당부서</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 책임자</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 등록일</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 수정일</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 상태코드</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 비고</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 연락처</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 분류</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 우선순위</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 진척률</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 예산코드</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">주소록 승인자</dt>
							<dd>{addrbookRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 63: 즐겨찾기관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>즐겨찾기관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedFavorite}
							onValueChange={setSelectedFavorite}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="즐겨찾기 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{favoriteStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchFavorite}
							onChange={(e) => setSearchFavorite(e.target.value)}
							placeholder="즐겨찾기 검색..."
						/>
						<Button size="sm">즐겨찾기 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{favoriteRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{favoritePending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>즐겨찾기 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">즐겨찾기 전체</div>
							<div className="text-2xl font-semibold">{favoriteRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">즐겨찾기 진행</div>
							<div className="text-2xl font-semibold">{favoriteRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">즐겨찾기 대기</div>
							<div className="text-2xl font-semibold">{favoriteRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">즐겨찾기 완료</div>
							<div className="text-2xl font-semibold">{favoriteRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>즐겨찾기 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 담당부서</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 책임자</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 등록일</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 수정일</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 상태코드</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 비고</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 연락처</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 분류</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 우선순위</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 진척률</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 예산코드</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">즐겨찾기 승인자</dt>
							<dd>{favoriteRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>

			{/* ===== 섹션 64: 설정관리 ===== */}
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>설정관리</CardTitle>
				</CardHeader>
				<CardContent className="space-y-4">
					<div className="flex flex-wrap gap-2">
						<Select
							value={selectedSetting}
							onValueChange={setSelectedSetting}
						>
							<SelectTrigger className="w-44">
								<SelectValue placeholder="설정 상태 선택" />
							</SelectTrigger>
							<SelectContent>
								{settingStatusOptions.map((opt) => (
									<SelectItem
										key={opt.value}
										value={opt.value}
									>
										{opt.label}
									</SelectItem>
								))}
							</SelectContent>
						</Select>
						<Input
							className="h-9 w-48"
							value={searchSetting}
							onChange={(e) => setSearchSetting(e.target.value)}
							placeholder="설정 검색..."
						/>
						<Button size="sm">설정 등록</Button>
					</div>
					<table className="w-full text-sm">
						<thead>
							<tr>
								<th className="text-left">이름</th>
								<th className="text-left">상태</th>
								<th className="text-left">담당자</th>
								<th className="text-right">금액</th>
								<th className="text-left">수정일</th>
							</tr>
						</thead>
						<tbody>
							{settingRows.map((row) => (
								<tr
									key={row.id}
									className="border-t"
								>
									<td>{row.name}</td>
									<td>{row.status}</td>
									<td>{row.owner}</td>
									<td className="text-right">{row.amount.toLocaleString()}</td>
									<td>{row.updated_at}</td>
								</tr>
							))}
							{settingPending && (
								<tr>
									<td colSpan={5}>불러오는 중…</td>
								</tr>
							)}
						</tbody>
					</table>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>설정 요약</CardTitle>
				</CardHeader>
				<CardContent>
					<div className="grid grid-cols-4 gap-3">
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">설정 전체</div>
							<div className="text-2xl font-semibold">{settingRows.length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">설정 진행</div>
							<div className="text-2xl font-semibold">{settingRows.filter((r) => r.status === 'active').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">설정 대기</div>
							<div className="text-2xl font-semibold">{settingRows.filter((r) => r.status === 'pending').length}</div>
						</div>
						<div className="rounded border p-3">
							<div className="text-xs text-muted-foreground">설정 완료</div>
							<div className="text-2xl font-semibold">{settingRows.filter((r) => r.status === 'done').length}</div>
						</div>
					</div>
				</CardContent>
			</Card>
			<Card className="border shadow-sm">
				<CardHeader>
					<CardTitle>설정 상세</CardTitle>
				</CardHeader>
				<CardContent>
					<dl className="grid grid-cols-2 gap-x-6 gap-y-2 text-sm">
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 담당부서</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 책임자</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 등록일</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 수정일</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 상태코드</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 비고</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 연락처</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 분류</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 우선순위</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 진척률</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 예산코드</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
						<div className="flex justify-between border-b py-1">
							<dt className="text-muted-foreground">설정 승인자</dt>
							<dd>{settingRows[0]?.name ?? '-'}</dd>
						</div>
					</dl>
				</CardContent>
			</Card>
		</div>
	);
}
